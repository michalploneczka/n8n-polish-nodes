---
phase: 15-integracja-z-https-api-nfz-gov-pl-app-itl-api
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - packages/n8n-nodes-nfz/__tests__/Nfz.node.test.ts
  - packages/n8n-nodes-nfz/nodes/Nfz/Nfz.node.json
  - packages/n8n-nodes-nfz/README.md
autonomous: true
requirements:
  - NFZ-09
  - NFZ-10

must_haves:
  truths:
    - "All nock-based tests pass for description validation and HTTP contract"
    - "Codex file categorizes node under Healthcare"
    - "README documents all operations, credentials (none), and includes example workflow"
    - "Build and lint pass cleanly"
  artifacts:
    - path: "packages/n8n-nodes-nfz/__tests__/Nfz.node.test.ts"
      provides: "Description tests + nock HTTP contract tests"
      min_lines: 100
    - path: "packages/n8n-nodes-nfz/nodes/Nfz/Nfz.node.json"
      provides: "Codex metadata"
      contains: "Healthcare"
    - path: "packages/n8n-nodes-nfz/README.md"
      provides: "Package documentation"
      contains: "## Operations"
  key_links:
    - from: "packages/n8n-nodes-nfz/__tests__/Nfz.node.test.ts"
      to: "packages/n8n-nodes-nfz/nodes/Nfz/Nfz.node.ts"
      via: "import { Nfz }"
      pattern: "import.*Nfz.*from.*Nfz.node"
---

<objective>
Add tests, codex metadata, and README for the NFZ node to complete the package for npm publishing.

Purpose: Ensure code quality via nock-based tests, discoverability via codex metadata, and usability via documentation.
Output: Test file with description + HTTP contract tests, codex JSON, README with operations table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-integracja-z-https-api-nfz-gov-pl-app-itl-api/15-RESEARCH.md
@.planning/phases/15-integracja-z-https-api-nfz-gov-pl-app-itl-api/15-01-SUMMARY.md

<interfaces>
<!-- Reference: NBP test pattern -->

From packages/n8n-nodes-nbp/__tests__/Nbp.node.test.ts (structure):
```typescript
import { setupNock, teardownNock, createNockScope } from '@n8n-polish-nodes/test-utils';
import { Nbp } from '../nodes/Nbp/Nbp.node';

const BASE_URL = 'https://api.nbp.pl';

describe('Nbp Node', () => {
  let node: Nbp;
  beforeAll(() => { node = new Nbp(); });

  describe('Node Description', () => {
    it('should have displayName "NBP Exchange Rates"', () => { ... });
    it('should have name "nbp"', () => { ... });
    it('should have correct requestDefaults baseURL', () => { ... });
    it('should NOT have credentials', () => { ... });
    it('should have N resources', () => { ... });
    it('should have N operations for resource X', () => { ... });
  });

  describe('HTTP Contract Tests', () => {
    beforeAll(setupNock);
    afterAll(teardownNock);
    // nock-based contract verification
  });
});
```

From packages/n8n-nodes-nbp/nodes/Nbp/Nbp.node.json (codex pattern):
```json
{
  "node": "n8n-nodes-nbp.nbp",
  "nodeVersion": "1.0",
  "codexVersion": "1.0",
  "categories": ["Data & Storage"],
  "subcategories": { "Data & Storage": ["Other Data Stores"] },
  "resources": {
    "primaryDocumentation": [{ "url": "https://api.nbp.pl/" }]
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nock-based tests for node description and HTTP contracts</name>
  <files>packages/n8n-nodes-nfz/__tests__/Nfz.node.test.ts</files>
  <read_first>
    packages/n8n-nodes-nfz/nodes/Nfz/Nfz.node.ts,
    packages/n8n-nodes-nbp/__tests__/Nbp.node.test.ts,
    shared/test-utils/index.ts
  </read_first>
  <action>
    Create comprehensive test file following NBP test pattern. Include:

    **Description tests (unit, no HTTP):**
    1. `should have displayName "NFZ Treatment Waiting Times"` -- assert `node.description.displayName`
    2. `should have name "nfz"` -- assert `node.description.name`
    3. `should have correct requestDefaults baseURL` -- assert `node.description.requestDefaults?.baseURL` equals `'https://api.nfz.gov.pl/app-itl-api'`
    4. `should have api-version 1.3 in requestDefaults qs` -- assert `node.description.requestDefaults?.qs?.['api-version']` equals `'1.3'`
    5. `should have format json in requestDefaults qs` -- assert `node.description.requestDefaults?.qs?.format` equals `'json'`
    6. `should NOT have credentials` -- assert credentials is undefined or empty array (use same pattern as NBP: `creds === undefined || (Array.isArray(creds) && creds.length === 0)`)
    7. `should have 4 resources (queue, benefit, locality, province)` -- find resource property, assert 4 options, check values contain queue/benefit/locality/province
    8. `should have 3 queue operations (search, get, getManyPlaces)` -- find operation property with displayOptions queue, assert 3 options
    9. `should have 1 benefit operation (search)` -- find operation property with displayOptions benefit, assert 1 option
    10. `should have 1 locality operation (search)` -- find operation property with displayOptions locality, assert 1 option
    11. `should have 1 province operation (getAll)` -- find operation property with displayOptions province, assert 1 option
    12. `should have 16 province options plus All Provinces` -- find province field, assert 17 options total (16 voivodeships + "All Provinces")
    13. `should have case type field with Stable and Urgent options` -- find case field, assert 2 options with values 1 and 2

    **HTTP Contract tests (nock-based):**
    Use `setupNock`, `teardownNock`, `createNockScope` from `@n8n-polish-nodes/test-utils`.
    `BASE_URL = 'https://api.nfz.gov.pl'`, `API_PATH = '/app-itl-api'`

    14. `GET /queues returns queue search results` -- nock intercept GET `/app-itl-api/queues` with query params `{ case: '1', 'api-version': '1.3', format: 'json' }`, reply 200 with JSON API response fixture:
    ```json
    {
      "meta": { "count": 1, "page": 1, "limit": 25 },
      "links": { "self": "/app-itl-api/queues?case=1" },
      "data": [{ "type": "queue", "id": "test-guid-123", "attributes": { "case": 1, "benefit": "PORADNIA TESTOWA", "provider": "TEST PROVIDER", "locality": "WARSZAWA" } }]
    }
    ```
    Assert response status 200 and body contains data array.

    15. `GET /queues/{id} returns single queue` -- nock intercept GET `/app-itl-api/queues/test-guid-123`, reply 200 with single queue object.

    16. `GET /benefits returns benefit search results` -- nock intercept GET `/app-itl-api/benefits` with query `{ name: 'poradnia', 'api-version': '1.3', format: 'json' }`, reply 200 with benefits fixture.

    17. `GET /localities returns locality search results` -- nock intercept GET `/app-itl-api/localities` with query `{ name: 'Warszawa', 'api-version': '1.3', format: 'json' }`, reply 200 with localities fixture.

    18. `GET /provinces returns province list` -- nock intercept GET `/app-itl-api/provinces`, reply 200 with provinces fixture.

    19. `handles 404 error response` -- nock intercept GET `/app-itl-api/queues/nonexistent`, reply 404 with `{ "meta": { "message": "Not found" } }`. Assert nock was called (request was made).

    Use `// @ts-ignore` comment before test-utils import (same as NBP pattern).
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-nfz && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `import { Nfz } from '../nodes/Nfz/Nfz.node'`
    - Test file contains at least 15 test cases (`it(` or `test(`)
    - Test file contains `setupNock` and `teardownNock` calls
    - Test file verifies `displayName` equals `'NFZ Treatment Waiting Times'`
    - Test file verifies `baseURL` equals `'https://api.nfz.gov.pl/app-itl-api'`
    - Test file verifies `api-version` equals `'1.3'`
    - Test file verifies no credentials
    - Test file verifies 4 resources
    - Test file contains nock intercepts for `/queues`, `/benefits`, `/localities`, `/provinces`
    - `npm test` passes with 0 failures
  </acceptance_criteria>
  <done>All tests pass -- description validation and HTTP contract tests confirm node structure and API routing.</done>
</task>

<task type="auto">
  <name>Task 2: Codex, README, build + lint verification</name>
  <files>
    packages/n8n-nodes-nfz/nodes/Nfz/Nfz.node.json,
    packages/n8n-nodes-nfz/README.md
  </files>
  <read_first>
    packages/n8n-nodes-nbp/nodes/Nbp/Nbp.node.json,
    packages/n8n-nodes-nbp/README.md,
    packages/n8n-nodes-nfz/nodes/Nfz/Nfz.node.ts
  </read_first>
  <action>
    **Codex file (Nfz.node.json):**
    ```json
    {
      "node": "n8n-nodes-nfz.nfz",
      "nodeVersion": "1.0",
      "codexVersion": "1.0",
      "categories": ["Miscellaneous"],
      "subcategories": {
        "Miscellaneous": ["Other"]
      },
      "resources": {
        "primaryDocumentation": [
          { "url": "https://api.nfz.gov.pl/app-itl-api/" }
        ]
      }
    }
    ```
    Note: n8n does not have a "Healthcare" category. Use "Miscellaneous" > "Other" as the closest match, similar to how government/public data nodes are categorized. If during build/lint a different category is required, adjust accordingly.

    **README.md** -- follow NBP README structure:

    # n8n-nodes-nfz

    This is an n8n community node for the [NFZ Treatment Waiting Times API](https://api.nfz.gov.pl/app-itl-api/) (Informator o Terminach Leczenia).

    Search healthcare treatment waiting times, providers, and service dictionaries from NFZ (National Health Fund of Poland). No API key or registration required -- this is a public open data API.

    [n8n community nodes docs](https://docs.n8n.io/integrations/community-nodes/)

    ## Operations

    ### Queue
    | Operation | Description |
    |-----------|-------------|
    | Search | Search treatment waiting times by case type, province, benefit, locality |
    | Get | Get details of a specific queue entry by ID |
    | Get Many Places | Get alternative appointment locations for a queue entry |

    ### Benefit (Dictionary)
    | Operation | Description |
    |-----------|-------------|
    | Search | Search healthcare benefit/service names |

    ### Locality (Dictionary)
    | Operation | Description |
    |-----------|-------------|
    | Search | Search locality names |

    ### Province (Dictionary)
    | Operation | Description |
    |-----------|-------------|
    | Get All | List all 16 Polish voivodeships |

    ## Credentials

    No credentials required. The NFZ API is a public open data service.

    ## Usage Notes

    - **Case Type** is required for Queue Search: Stable (stabilny) or Urgent (pilny)
    - **Province** dropdown includes all 16 Polish voivodeships for easy filtering
    - Responses follow JSON API format (`data[].attributes` contains the actual data)
    - Rate limit: 10 requests per second -- add Wait nodes in loops if needed
    - API version 1.3 is used automatically

    ## Compatibility

    - Tested with n8n 1.x
    - Requires n8n >= 0.198.0

    ## Installation

    ### Community Nodes (recommended)
    1. Go to **Settings > Community Nodes**
    2. Select **Install**
    3. Enter `n8n-nodes-nfz`
    4. Agree to risks and **Install**

    ### Manual Installation
    ```bash
    cd ~/.n8n/nodes
    npm install n8n-nodes-nfz
    ```

    ## Resources

    - [NFZ API Documentation](https://api.nfz.gov.pl/app-itl-api/)
    - [n8n Community Nodes docs](https://docs.n8n.io/integrations/community-nodes/)
    - [NFZ Official Website](https://www.nfz.gov.pl/)

    ## License

    [MIT](LICENSE.md)

    **After creating both files, run build and lint:**
    ```bash
    cd packages/n8n-nodes-nfz && npm run build && npm run lint
    ```
    Fix any lint errors before completing.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-nfz && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -5 && echo "BUILD_LINT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Nfz.node.json contains `"node": "n8n-nodes-nfz.nfz"`
    - Nfz.node.json contains `"categories"` array with a valid n8n category
    - Nfz.node.json contains `"url": "https://api.nfz.gov.pl/app-itl-api/"`
    - README.md contains `## Operations`
    - README.md contains `## Credentials` section mentioning no credentials required
    - README.md contains `n8n-nodes-nfz` in installation section
    - README.md contains `## Usage Notes` with rate limit info
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - dist/ directory contains compiled JS files after build
  </acceptance_criteria>
  <done>Codex, README created. Build compiles to dist/ without errors. Lint passes cleanly. Package ready for npm publish.</done>
</task>

</tasks>

<verification>
- `cd packages/n8n-nodes-nfz && npm test` -- all tests pass
- `cd packages/n8n-nodes-nfz && npm run build` -- compiles without errors
- `cd packages/n8n-nodes-nfz && npm run lint` -- no lint errors
- `ls packages/n8n-nodes-nfz/dist/nodes/Nfz/Nfz.node.js` -- compiled output exists
</verification>

<success_criteria>
- All nock-based tests pass (description + HTTP contracts)
- Codex file has valid category and documentation URL
- README documents all operations, no-credentials requirement, and usage notes
- Build and lint pass with zero errors
- Package is complete and ready for npm publish
</success_criteria>

<output>
After completion, create `.planning/phases/15-integracja-z-https-api-nfz-gov-pl-app-itl-api/15-02-SUMMARY.md`
</output>
