---
phase: 01-monorepo-bootstrap-smsapi-ceidg
plan: 07
type: execute
wave: 4
depends_on: [01-02, 01-05]
files_modified:
  - packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts
autonomous: true
requirements: [SMSAPI-08, SMSAPI-09]

must_haves:
  truths:
    - "Tests verify format=json is in requestDefaults.qs"
    - "Tests verify all 9 operations exist across 4 resources"
    - "Tests verify SMS Send routes to POST /sms.do"
    - "Tests verify Contacts operations route to /contacts endpoints"
    - "Tests mock HTTP calls with nock — no real API requests"
    - "Error handling tests verify 401 and 400 responses produce NodeApiError"
  artifacts:
    - path: "packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts"
      provides: "SMSAPI nock-based tests"
      contains: "nock"
      min_lines: 80
  key_links:
    - from: "packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts"
      to: "shared/test-utils"
      via: "import"
      pattern: "@n8n-polish-nodes/test-utils"
---

<objective>
Create nock-based tests for the SMSAPI node covering all 4 resources, format=json injection verification, and error handling.

Purpose: Verify SMSAPI node description correctness, format=json in requestDefaults (critical per D-15), and error handling. Validate shared test-utils work with a larger node.
Output: packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md

<interfaces>
From packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts:
- Resources: sms, contact, group, account
- Operations per resource:
  - sms: send, sendGroup, getReport
  - contact: list, create, update, delete
  - group: list
  - account: getBalance
- baseURL: https://api.smsapi.pl
- requestDefaults.qs: { format: 'json' }

From shared/test-utils:
- createMockExecuteFunctions, setupNock, teardownNock, createNockScope
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SMSAPI nock tests for all resources and error handling</name>
  <files>packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts</files>
  <read_first>
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/resources/contacts.ts
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/resources/groups.ts
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/resources/account.ts
    /Users/mploneczka/projects/n8n/shared/test-utils/index.ts
  </read_first>
  <action>
Create `packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts` with comprehensive tests.

```typescript
import { setupNock, teardownNock, createNockScope } from '@n8n-polish-nodes/test-utils';
import { NodeApiError } from 'n8n-workflow';
import { Smsapi } from '../nodes/Smsapi/Smsapi.node';
import { SmsapiApi } from '../credentials/SmsapiApi.credentials';
```

**describe('Smsapi Node'):**

**describe('Request Defaults'):**
- test: 'should have correct baseURL' — assert baseURL is 'https://api.smsapi.pl'
- test: 'should inject format=json in requestDefaults.qs' — THIS IS CRITICAL (per D-15). Assert `description.requestDefaults.qs.format === 'json'`. If this test fails, all legacy /sms.do calls will return non-JSON.
- test: 'should set Accept and Content-Type headers' — verify JSON headers in requestDefaults

**describe('Resources'):**
- test: 'should have 4 resources: sms, contact, group, account' — find resource property, assert 4 options with correct values

**describe('SMS Operations'):**
- test: 'should have 3 SMS operations: send, sendGroup, getReport' — find operation property with displayOptions show resource=sms, verify 3 options
- test: 'Send operation routes to POST /sms.do' — find `to` field's routing, verify method POST and url /sms.do
- test: 'Send operation has required fields: to, message' — verify both have required: true
- test: 'Send operation has Additional Fields with from, encoding, date, test' — verify collection options
- test: 'Send Group routes to POST /sms.do with group param' — find group field routing
- test: 'Get Report routes to GET /sms.do' — find messageId field routing, verify GET method

**describe('Contacts Operations'):**
- test: 'should have 4 contact operations: list, create, update, delete' — verify all exist
- test: 'List routes to GET /contacts' — verify routing
- test: 'Create routes to POST /contacts' — verify routing
- test: 'Update routes to PUT /contacts/{id}' — verify routing contains /contacts/ with parameter interpolation
- test: 'Delete routes to DELETE /contacts/{id}' — verify routing

**describe('Groups Operations'):**
- test: 'should have List operation routing to GET /contacts/groups'

**describe('Account Operations'):**
- test: 'should have Get Balance operation routing to GET /profile'

**describe('SmsapiApi Credentials'):**
- test: 'should use Bearer token authentication' — assert authenticate header contains 'Bearer'
- test: 'should test connection via /profile endpoint' — assert test.request.url is '/profile'
- test: 'should include format=json in test request' — assert test.request.qs.format is 'json'

**describe('No Execute Method'):**
- test: 'should not have execute method (declarative only per D-16)' — assert no execute property on node instance

**describe('HTTP Integration'):**
beforeEach/afterEach: setupNock/teardownNock

- test: 'credential test endpoint responds to GET /profile' — nock `https://api.smsapi.pl` GET `/profile` query includes format=json, return 200 with `{ name: 'test', points: 100 }`. Verify nock.isDone().

- test: 'handles 401 unauthorized — produces NodeApiError with English message' — nock returns 401 with `{ message: 'Authorization failed' }`. Import `NodeApiError` from `n8n-workflow`. Create a `NodeApiError` with the 401 response data and a mock node object. Assert: (1) nock was called (nock.isDone()), (2) the error is an instance of `NodeApiError`, (3) the error message is a non-empty English string. This validates SMSAPI-08: HTTP errors produce NodeApiError with readable English messages. Note: for declarative nodes, n8n runtime wraps HTTP errors automatically — this test validates the error class works with SMSAPI error payloads.

- test: 'handles 400 bad request for SMS send — produces NodeApiError with English message' — nock POST `/sms.do` returns 400 with `{ error: 101, message: 'Authorization failed' }`. Create a `NodeApiError` from this response. Assert: (1) nock was called, (2) error is `NodeApiError` instance, (3) error message is non-empty English string. Validates SMSAPI-08.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi && npx jest --config jest.config.js --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Test file imports from `@n8n-polish-nodes/test-utils`
    - Test file imports `Smsapi` from `../nodes/Smsapi/Smsapi.node`
    - Test file imports `NodeApiError` from `n8n-workflow`
    - Test file contains explicit test for `requestDefaults.qs.format === 'json'` (D-15 validation)
    - Test file verifies 4 resources exist (sms, contact, group, account)
    - Test file verifies 9 operations across all resources
    - Test file verifies SMS Send routes to POST /sms.do
    - Test file contains `setupNock()` and `teardownNock()`
    - Test file contains `nock('https://api.smsapi.pl')` or `createNockScope`
    - Test file has at least one 401 and one 400 error test that assert NodeApiError instance and English message
    - All tests pass when run with `npx jest`
  </acceptance_criteria>
  <done>SMSAPI tests pass: format=json verified in requestDefaults, all 9 operations validated, error handling tested with NodeApiError assertion (SMSAPI-08)</done>
</task>

</tasks>

<verification>
- `cd packages/n8n-nodes-smsapi && npx jest --config jest.config.js` passes
- `grep "format.*json" packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts` confirms format=json test
- `grep "NodeApiError" packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts` confirms NodeApiError assertion exists
- `grep -c "test\|it(" packages/n8n-nodes-smsapi/__tests__/Smsapi.node.test.ts` shows 15+ test cases
</verification>

<success_criteria>
SMSAPI test suite passes. format=json injection is verified. All 4 resources and 9 operations are structurally validated. Error handling tested with NodeApiError assertion (SMSAPI-08).
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-07-SUMMARY.md`
</output>
