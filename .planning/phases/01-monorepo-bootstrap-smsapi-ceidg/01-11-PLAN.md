---
phase: 01-monorepo-bootstrap-smsapi-ceidg
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts
autonomous: true
gap_closure: true
requirements:
  - CEIDG-06
  - CEIDG-07

must_haves:
  truths:
    - "All 14 CEIDG tests pass with 0 failures"
    - "Test assertions match actual v3 API implementation (baseURL, endpoints, credential auth)"
    - "HTTP integration tests use v3 API path /api/ceidg/v3/firma for NIP lookup"
  artifacts:
    - path: "packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts"
      provides: "Updated CEIDG tests aligned with v3 API"
      contains: "/api/ceidg/v3"
  key_links:
    - from: "packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts"
      to: "packages/n8n-nodes-ceidg/nodes/Ceidg/Ceidg.node.ts"
      via: "test assertions match node description values"
      pattern: "api/ceidg/v3"
    - from: "packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts"
      to: "packages/n8n-nodes-ceidg/credentials/CeidgApi.credentials.ts"
      via: "test assertions match credential auth and test URL"
      pattern: "\\$credentials\\.apiKey"
---

<objective>
Fix 5 stale test assertions in Ceidg.node.test.ts that reference the deprecated v2 API, aligning them with the actual v3 implementation validated against the live CEIDG API.

Purpose: Close the verification gap — CEIDG tests currently fail (5/14), blocking CI pipeline. The node implementation is correct (confirmed via real API in plan 01-10); only tests are stale.
Output: All 14 CEIDG tests passing; `pnpm run test:all` exits 0.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CEIDG test assertions to match v3 API implementation</name>
  <files>packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts</files>

  <read_first>
    packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts
    packages/n8n-nodes-ceidg/nodes/Ceidg/Ceidg.node.ts
    packages/n8n-nodes-ceidg/credentials/CeidgApi.credentials.ts
  </read_first>

  <action>
  Update 5 failing assertions plus the API_PATH constant in Ceidg.node.test.ts. The node uses CEIDG API v3 (confirmed live), but tests still assert v2 values. All changes are in the test file only — the node and credential files are correct.

  **Change 1 — Line 9: API_PATH constant**
  OLD: `const API_PATH = '/api/ceidg/v2';`
  NEW: `const API_PATH = '/api/ceidg/v3';`

  **Change 2 — Line 24-26: baseURL assertion**
  OLD: `expect(node.description.requestDefaults?.baseURL).toBe('https://dane.biznes.gov.pl/api/ceidg/v2',);`
  NEW: `expect(node.description.requestDefaults?.baseURL).toBe('https://dane.biznes.gov.pl/api/ceidg/v3',);`

  **Change 3 — Line 66: NIP routing url assertion**
  OLD: `expect(nipProp!.routing!.request).toEqual(expect.objectContaining({ url: '/firmy', method: 'GET' }),);`
  NEW: `expect(nipProp!.routing!.request).toEqual(expect.objectContaining({ url: '/firma', method: 'GET' }),);`

  **Change 4 — Line 91: Get routing url assertion**
  OLD: `expect(companyIdProp!.routing!.request!.url).toContain('/firmy/');`
  NEW: `expect(companyIdProp!.routing!.request!.url).toContain('/firma/');`

  **Change 5 — Line 118: Credential auth assertion**
  OLD: `expect(auth.properties.headers.Authorization).toContain('$credentials?.apiKey');`
  NEW: `expect(auth.properties.headers.Authorization).toContain('$credentials.apiKey');`

  **Change 6 — Lines 125-126: Credential test URL assertion**
  OLD:
  ```
  expect(test.request.url).toBe('/firmy');
  expect(test.request.method).toBe('GET');
  ```
  NEW:
  ```
  expect(test.request.url).toBe('/firma?nip=6351723862');
  expect(test.request.method).toBe('GET');
  ```

  **Change 7 — HTTP integration test nock URLs (lines 141, 154, 174, 181, 215, 222)**
  All nock intercepts and https.get calls use `${API_PATH}/firmy` which resolves to `/api/ceidg/v2/firmy`. After Change 1 (API_PATH becomes v3), the path becomes `/api/ceidg/v3/firmy`. But the actual NIP endpoint is `/firma` not `/firmy`. Update all 6 occurrences:
  OLD: `${API_PATH}/firmy`
  NEW: `${API_PATH}/firma`

  Note: The "Search by Name" operation genuinely uses `/firmy` (plural) for name search. The HTTP integration tests all test NIP lookup which uses `/firma` (singular). This is correct — the node has two different endpoints: `/firma` for NIP and `/firmy` for name search.

  Also update the test description at line 121 to match:
  OLD: `it('should have test request for /firmy endpoint', () => {`
  NEW: `it('should have test request for /firma endpoint', () => {`

  **Regarding publish dry-run (Gap 2):** No code change needed. The `n8n-node prerelease` script intentionally blocks local publishes to enforce the GitHub Actions publish path. This is expected behavior per @n8n/node-cli design. Document this in the SUMMARY as a known constraint, not a bug.
  </action>

  <verify>
    <automated>cd /Users/mploneczka/projects/n8n && pnpm --filter n8n-nodes-ceidg test 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - grep -c "api/ceidg/v2" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts returns 0
  - grep -c "api/ceidg/v3" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts returns at least 2
  - grep -c "url: '/firmy'" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts returns 1 (only the Search by Name test at line 78)
  - grep "\\$credentials?\\.apiKey" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts returns no matches
  - grep "\\$credentials\\.apiKey" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts returns 1 match
  - grep "/firma?nip=6351723862" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts returns 1 match
  - pnpm --filter n8n-nodes-ceidg test exits 0 with "14 passed" in output
  - pnpm run test:all exits 0 (both SMSAPI and CEIDG tests pass)
  </acceptance_criteria>

  <done>All 14 CEIDG tests pass. All assertions match the actual v3 API implementation. Zero references to v2 API remain in test file. Full test suite (test:all) exits 0.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter n8n-nodes-ceidg test` — all 14 tests pass, 0 failures
2. `pnpm run test:all` — both CEIDG (14) and SMSAPI (24) tests pass
3. `grep -c "v2" packages/n8n-nodes-ceidg/__tests__/Ceidg.node.test.ts` — returns 0
4. No changes to node implementation files (only test file modified)
</verification>

<success_criteria>
- 14/14 CEIDG tests pass (was 9/14)
- 38/38 total tests pass across monorepo (24 SMSAPI + 14 CEIDG)
- CEIDG-06 (NodeApiError tests) status changes from PARTIALLY_SATISFIED to SATISFIED
- CEIDG-07 (nock tests) status changes from BLOCKED to SATISFIED
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-11-SUMMARY.md`
</output>
