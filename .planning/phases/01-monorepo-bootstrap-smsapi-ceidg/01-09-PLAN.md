---
phase: 01-monorepo-bootstrap-smsapi-ceidg
plan: 09
type: execute
wave: 5
depends_on: [01-03, 01-06, 01-07]
files_modified: []
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "pnpm install succeeds from root"
    - "pnpm run build:all compiles both packages without errors"
    - "pnpm run test:all runs all tests and they pass"
    - "pnpm run lint:all runs without errors"
    - "node scripts/verify-packages.js passes for both packages"
    - "copy-codex script places .node.json files in dist/"
  artifacts:
    - path: "packages/n8n-nodes-ceidg/dist/nodes/Ceidg/Ceidg.node.json"
      provides: "CEIDG codex file in dist (built)"
      contains: "codexVersion"
    - path: "packages/n8n-nodes-smsapi/dist/nodes/Smsapi/Smsapi.node.json"
      provides: "SMSAPI codex file in dist (built)"
      contains: "codexVersion"
  key_links:
    - from: "package.json"
      to: "packages/*/package.json"
      via: "pnpm workspaces"
      pattern: "build:all"
---

<objective>
Run the full monorepo build pipeline: install, build, lint, test, verify. Fix any issues that surface.

Purpose: Validate that all infrastructure and node code works together as an integrated monorepo. This is the first time everything runs end-to-end.
Output: Green build across all commands; any fixes needed applied

Note: This is an integration-validation plan. Build and test commands may take 60-120 seconds on cold start. Verification timeout should be set to 180000ms.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and verify workspace resolution</name>
  <files></files>
  <read_first>
    /Users/mploneczka/projects/n8n/package.json
    /Users/mploneczka/projects/n8n/pnpm-workspace.yaml
  </read_first>
  <action>
Run from project root:
1. `pnpm install` — verify all dependencies resolve, workspace packages are linked
2. Verify `node_modules/.pnpm` exists (pnpm store)
3. Verify workspace packages are recognized: `pnpm list --recursive --depth 0`

If pnpm install fails:
- Check .npmrc settings (shamefully-hoist, strict-peer-dependencies)
- Check if @n8n/node-cli version exists on npm registry
- Check pnpm-workspace.yaml glob patterns match actual directory structure

Fix any issues that arise during installation.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n && pnpm install --frozen-lockfile 2>&1 | tail -3 || pnpm install 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits with code 0
    - `pnpm list --recursive --depth 0` shows n8n-nodes-ceidg and n8n-nodes-smsapi as workspace packages
    - node_modules directory exists
  </acceptance_criteria>
  <done>All dependencies installed, workspace packages recognized by pnpm</done>
</task>

<task type="auto">
  <name>Task 2: Run build, lint, test, and verify-packages pipeline</name>
  <files></files>
  <read_first>
    /Users/mploneczka/projects/n8n/package.json
    /Users/mploneczka/projects/n8n/scripts/verify-packages.js
    /Users/mploneczka/projects/n8n/scripts/copy-codex.js
  </read_first>
  <action>
Run the full CI-equivalent pipeline from project root:

1. `pnpm run build:all` — compiles both packages
   After build, verify codex files were copied:
   - Check `packages/n8n-nodes-ceidg/dist/nodes/Ceidg/Ceidg.node.json` exists
   - Check `packages/n8n-nodes-smsapi/dist/nodes/Smsapi/Smsapi.node.json` exists

2. `pnpm run lint:all` — runs n8n-node lint on both packages
   If lint errors occur, fix them. Common issues:
   - Missing `// eslint-disable-next-line` for specific n8n patterns
   - Import ordering
   - Unused variables

3. `pnpm run test:all` — runs Jest tests in both packages
   If tests fail, fix the test code or the source code.

4. `node scripts/verify-packages.js` — validates package.json fields for both packages

Fix ALL errors encountered during this pipeline. This task is not complete until all 4 commands exit with code 0.

If `n8n-node build` fails because it cannot find `pnpm`, ensure pnpm is installed globally or via corepack. If `n8n-node lint` has issues with ESLint config, check that eslint version in root devDeps matches what @n8n/node-cli expects.

Note: This pipeline may take 60-120 seconds total. Use timeout of 180000ms for verification commands.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n && pnpm run build:all && pnpm run test:all && node scripts/verify-packages.js && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build:all` exits 0 — both packages compile
    - `packages/n8n-nodes-ceidg/dist/nodes/Ceidg/Ceidg.node.json` exists after build
    - `packages/n8n-nodes-smsapi/dist/nodes/Smsapi/Smsapi.node.json` exists after build
    - `pnpm run test:all` exits 0 — all tests pass
    - `node scripts/verify-packages.js` exits 0 — both packages validated
  </acceptance_criteria>
  <done>Full monorepo pipeline passes: install, build (with codex copy), lint, test, verify-packages. Zero errors.</done>
</task>

</tasks>

<verification>
- `pnpm run build:all && pnpm run lint:all && pnpm run test:all && node scripts/verify-packages.js` all exit 0
- `ls packages/n8n-nodes-ceidg/dist/nodes/Ceidg/Ceidg.node.json` exists
- `ls packages/n8n-nodes-smsapi/dist/nodes/Smsapi/Smsapi.node.json` exists
</verification>

<success_criteria>
Complete monorepo pipeline passes. Both packages compile, lint, test, and pass verification. Codex files are in dist/. The project is in a shippable state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-09-SUMMARY.md`
</output>
