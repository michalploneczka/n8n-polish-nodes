---
phase: 01-monorepo-bootstrap-smsapi-ceidg
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - packages/n8n-nodes-smsapi/package.json
  - packages/n8n-nodes-smsapi/tsconfig.json
  - packages/n8n-nodes-smsapi/jest.config.js
  - packages/n8n-nodes-smsapi/credentials/SmsapiApi.credentials.ts
  - packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts
  - packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.json
  - packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts
  - packages/n8n-nodes-smsapi/nodes/Smsapi/resources/contacts.ts
  - packages/n8n-nodes-smsapi/nodes/Smsapi/resources/groups.ts
  - packages/n8n-nodes-smsapi/nodes/Smsapi/resources/account.ts
  - packages/n8n-nodes-smsapi/icons/smsapi.svg
autonomous: true
requirements: [SMSAPI-01, SMSAPI-02, SMSAPI-03, SMSAPI-04, SMSAPI-05, SMSAPI-06, SMSAPI-07, SMSAPI-10, SMSAPI-11, SMSAPI-12]

must_haves:
  truths:
    - "SMSAPI credential uses Bearer token in Authorization header"
    - "SMSAPI credential has test request to /profile endpoint"
    - "format=json is injected in requestDefaults.qs at node level, not per-operation"
    - "SMS Send operation sends POST to /sms.do with to, message, from, encoding, date, test fields"
    - "SMS Send Group operation sends POST to /sms.do with group parameter"
    - "SMS Get Report operation sends GET to /sms.do with query parameters"
    - "Contacts resource has List, Create, Update, Delete operations"
    - "Groups resource has List operation"
    - "Account resource has Balance operation hitting /profile"
    - "All operations are declarative — no execute() method"
  artifacts:
    - path: "packages/n8n-nodes-smsapi/credentials/SmsapiApi.credentials.ts"
      provides: "SMSAPI Bearer credential"
      contains: "Bearer"
    - path: "packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts"
      provides: "Main node file importing resource descriptions"
      contains: "requestDefaults"
    - path: "packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts"
      provides: "SMS operations (Send, Send Group, Get Report)"
      contains: "/sms.do"
    - path: "packages/n8n-nodes-smsapi/nodes/Smsapi/resources/contacts.ts"
      provides: "Contacts CRUD operations"
      contains: "/contacts"
    - path: "packages/n8n-nodes-smsapi/nodes/Smsapi/resources/groups.ts"
      provides: "Groups List operation"
      contains: "/contacts/groups"
    - path: "packages/n8n-nodes-smsapi/nodes/Smsapi/resources/account.ts"
      provides: "Account Balance operation"
      contains: "/profile"
  key_links:
    - from: "packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts"
      to: "packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts"
      via: "import and spread"
      pattern: "smsDescription"
    - from: "packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts"
      to: "https://api.smsapi.pl"
      via: "requestDefaults.baseURL"
      pattern: "api.smsapi.pl"
---

<objective>
Create the SMSAPI node package: credentials, declarative node with 4 resources (SMS, Contacts, Groups, Account) split into resource files, codex, placeholder icon, and package configuration.

Purpose: SMSAPI is the highest-priority node with confirmed demand. It is more complex than CEIDG (4 resources, 9 operations) and validates the resource-split file organization pattern.
Output: Complete packages/n8n-nodes-smsapi/ directory ready to build
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md
@.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-RESEARCH.md

<interfaces>
<!-- Canonical patterns from @n8n/node-cli template -->
From GithubIssues.node.ts:
```typescript
import { NodeConnectionTypes, type INodeType, type INodeTypeDescription } from 'n8n-workflow';
import { issueDescription } from './resources/issue';
export class GithubIssues implements INodeType {
  description: INodeTypeDescription = {
    requestDefaults: { baseURL: '...', headers: { ... } },
    properties: [resourceSelector, ...issueDescription, ...issueCommentDescription],
  };
}
```

From GithubIssuesApi.credentials.ts:
```typescript
authenticate: IAuthenticateGeneric = { type: 'generic', properties: { headers: { Authorization: '=token {{$credentials?.accessToken}}' } } };
test: ICredentialTestRequest = { request: { baseURL: '...', url: '/user', method: 'GET' } };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SMSAPI package config, credential, icon, and codex</name>
  <files>packages/n8n-nodes-smsapi/package.json, packages/n8n-nodes-smsapi/tsconfig.json, packages/n8n-nodes-smsapi/jest.config.js, packages/n8n-nodes-smsapi/credentials/SmsapiApi.credentials.ts, packages/n8n-nodes-smsapi/icons/smsapi.svg, packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.json</files>
  <read_first>
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg/package.json
    /Users/mploneczka/projects/n8nexamples/n8n/packages/@n8n/node-cli/src/template/templates/declarative/github-issues/template/credentials/GithubIssuesApi.credentials.ts
    /Users/mploneczka/projects/n8n/.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md
  </read_first>
  <action>
Create `packages/n8n-nodes-smsapi/` directory structure. Follow same patterns as CEIDG package but with SMSAPI-specific values.

**package.json** (same structure as CEIDG, different name/description):
- name: `"n8n-nodes-smsapi"`
- description: `"n8n community node for SMSAPI.pl - Polish SMS gateway"`
- keywords: `["n8n-community-node-package", "n8n", "smsapi", "sms", "poland", "polish"]`
- repository.directory: `"packages/n8n-nodes-smsapi"`
- n8n.credentials: `["dist/credentials/SmsapiApi.credentials.js"]`
- n8n.nodes: `["dist/nodes/Smsapi/Smsapi.node.js"]`
- Same scripts, devDependencies, peerDependencies as CEIDG package

**tsconfig.json:** Same as CEIDG — extends `../../tsconfig.base.json`, outDir `./dist/`, include credentials, nodes, package.json

**jest.config.js:** Same as CEIDG — extends base, moduleNameMapper for test-utils

**credentials/SmsapiApi.credentials.ts** (per D-13, D-14 — Bearer token):
```typescript
import type {
  IAuthenticateGeneric,
  Icon,
  ICredentialTestRequest,
  ICredentialType,
  INodeProperties,
} from 'n8n-workflow';

export class SmsapiApi implements ICredentialType {
  name = 'smsapiApi';
  displayName = 'SMSAPI API';
  icon: Icon = { light: 'file:../icons/smsapi.svg' };
  documentationUrl = 'https://www.smsapi.pl/docs';
  properties: INodeProperties[] = [
    {
      displayName: 'API Token',
      name: 'apiToken',
      type: 'string',
      typeOptions: { password: true },
      default: '',
      required: true,
      description: 'API token from SMSAPI.pl panel - API Settings',
    },
  ];

  authenticate: IAuthenticateGeneric = {
    type: 'generic',
    properties: {
      headers: {
        Authorization: '=Bearer {{$credentials?.apiToken}}',
      },
    },
  };

  test: ICredentialTestRequest = {
    request: {
      baseURL: 'https://api.smsapi.pl',
      url: '/profile',
      method: 'GET',
      qs: { format: 'json' },
    },
  };
}
```

**icons/smsapi.svg** (placeholder per D-09):
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" viewBox="0 0 60 60">
  <rect width="60" height="60" rx="8" fill="#00a651"/>
  <text x="30" y="35" text-anchor="middle" font-family="Arial,sans-serif" font-size="10" font-weight="bold" fill="white">SMSAPI</text>
</svg>
```

**nodes/Smsapi/Smsapi.node.json** (codex):
```json
{
  "node": "n8n-nodes-smsapi.smsapi",
  "nodeVersion": "1.0",
  "codexVersion": "1.0",
  "categories": ["Communication"],
  "subcategories": {
    "Communication": ["SMS"]
  },
  "resources": {
    "primaryDocumentation": [
      { "url": "https://www.smsapi.pl/docs" }
    ]
  }
}
```
  </action>
  <verify>
    <automated>grep -q "n8n-community-node-package" /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/package.json && grep -q "Bearer" /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/credentials/SmsapiApi.credentials.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"n8n-community-node-package"` keyword
    - package.json n8n.nodes contains `"dist/nodes/Smsapi/Smsapi.node.js"`
    - package.json repository.directory is `"packages/n8n-nodes-smsapi"`
    - SmsapiApi.credentials.ts authenticate header is `Authorization: '=Bearer {{$credentials?.apiToken}}'`
    - SmsapiApi.credentials.ts test request URL is `/profile` with `qs: { format: 'json' }`
    - Smsapi.node.json contains `"n8n-nodes-smsapi.smsapi"` and category `"Communication"`
    - icons/smsapi.svg exists with width/height 60
  </acceptance_criteria>
  <done>SMSAPI package structure created with Bearer credential, codex, placeholder icon</done>
</task>

<task type="auto">
  <name>Task 2: Create SMSAPI node with resource-split operations</name>
  <files>packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts, packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts, packages/n8n-nodes-smsapi/nodes/Smsapi/resources/contacts.ts, packages/n8n-nodes-smsapi/nodes/Smsapi/resources/groups.ts, packages/n8n-nodes-smsapi/nodes/Smsapi/resources/account.ts</files>
  <read_first>
    /Users/mploneczka/projects/n8nexamples/n8n/packages/@n8n/node-cli/src/template/templates/declarative/github-issues/template/nodes/GithubIssues/GithubIssues.node.ts
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/credentials/SmsapiApi.credentials.ts
    /Users/mploneczka/projects/n8n/.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md
  </read_first>
  <action>
Create the SMSAPI node using resource-split pattern (Research Pattern 3). All operations are declarative per D-16.

**nodes/Smsapi/Smsapi.node.ts** (main node file):
```typescript
import { NodeConnectionTypes, type INodeType, type INodeTypeDescription } from 'n8n-workflow';
import { smsDescription } from './resources/sms';
import { contactsDescription } from './resources/contacts';
import { groupsDescription } from './resources/groups';
import { accountDescription } from './resources/account';

export class Smsapi implements INodeType {
  description: INodeTypeDescription = {
    displayName: 'SMSAPI',
    name: 'smsapi',
    icon: { light: 'file:../../icons/smsapi.svg' },
    group: ['output'],
    version: 1,
    subtitle: '={{$parameter["operation"] + ": " + $parameter["resource"]}}',
    description: 'Send SMS messages and manage contacts via SMSAPI.pl',
    defaults: { name: 'SMSAPI' },
    usableAsTool: true,
    inputs: [NodeConnectionTypes.Main],
    outputs: [NodeConnectionTypes.Main],
    credentials: [{ name: 'smsapiApi', required: true }],
    requestDefaults: {
      baseURL: 'https://api.smsapi.pl',
      headers: {
        Accept: 'application/json',
        'Content-Type': 'application/json',
      },
      qs: {
        format: 'json',
      },
    },
    properties: [
      {
        displayName: 'Resource',
        name: 'resource',
        type: 'options',
        noDataExpression: true,
        options: [
          { name: 'SMS', value: 'sms' },
          { name: 'Contact', value: 'contact' },
          { name: 'Group', value: 'group' },
          { name: 'Account', value: 'account' },
        ],
        default: 'sms',
      },
      ...smsDescription,
      ...contactsDescription,
      ...groupsDescription,
      ...accountDescription,
    ],
  };
}
```

CRITICAL per D-15: `format: 'json'` is in `requestDefaults.qs` at node level. This ensures ALL requests (including legacy /sms.do) get format=json.

**nodes/Smsapi/resources/sms.ts** — exports `smsDescription: INodeProperties[]`:

Operation selector (displayOptions show resource=sms):
- Send (value: 'send')
- Send Group (value: 'sendGroup')
- Get Report (value: 'getReport')

**Send operation** fields (displayOptions show resource=sms, operation=send):
- `to` (string, required, description: 'Phone number in format 48XXXXXXXXX')
  routing: `{ request: { method: 'POST', url: '/sms.do' }, send: { type: 'body', property: 'to' } }`
- `message` (string, required, typeOptions: { rows: 3 }, description: 'SMS text content (max 160 chars for single SMS)')
  routing: `{ send: { type: 'body', property: 'message' } }`
- Additional Fields (collection): `from` (string, maxLength 11), `encoding` (options: default/utf-8), `date` (dateTime), `test` (boolean, description: 'Test mode - does not send, does not charge')
  routing on each leaf property inside the collection (NOT on the collection itself)

**Send Group operation** fields (displayOptions show resource=sms, operation=sendGroup):
- `group` (string, required, description: 'Contact group name')
  routing: `{ request: { method: 'POST', url: '/sms.do' }, send: { type: 'body', property: 'group' } }`
- `message` (string, required)
  routing: `{ send: { type: 'body', property: 'message' } }`
- Same Additional Fields as Send (from, encoding, date, test)

**Get Report operation** fields (displayOptions show resource=sms, operation=getReport):
- `messageId` (string, required, description: 'ID of the sent SMS message')
  routing: `{ request: { method: 'GET', url: '/sms.do' }, send: { type: 'query', property: 'id' } }`

**nodes/Smsapi/resources/contacts.ts** — exports `contactsDescription: INodeProperties[]`:

Operation selector (displayOptions show resource=contact):
- List (value: 'list')
- Create (value: 'create')
- Update (value: 'update')
- Delete (value: 'delete')

**List** — routing: `{ request: { method: 'GET', url: '/contacts' } }` (on operation selector option)
**Create** — routing: `{ request: { method: 'POST', url: '/contacts' } }`
  Fields: `phone_number` (string, required), Additional Fields collection with: `email`, `first_name`, `last_name`, `description`, `group_id`
**Update** — routing: `{ request: { method: 'PUT', url: '=/contacts/{{$parameter["contactId"]}}' } }`
  Fields: `contactId` (string, required), update fields collection
**Delete** — routing: `{ request: { method: 'DELETE', url: '=/contacts/{{$parameter["contactId"]}}' } }`
  Fields: `contactId` (string, required)

**nodes/Smsapi/resources/groups.ts** — exports `groupsDescription: INodeProperties[]`:

Operation selector (displayOptions show resource=group):
- List (value: 'list')

**List** — routing: `{ request: { method: 'GET', url: '/contacts/groups' } }`

**nodes/Smsapi/resources/account.ts** — exports `accountDescription: INodeProperties[]`:

Operation selector (displayOptions show resource=account):
- Get Balance (value: 'getBalance')

**Get Balance** — routing: `{ request: { method: 'GET', url: '/profile' } }`

No `execute()` method on the main node class. All operations are purely declarative per D-16.
  </action>
  <verify>
    <automated>grep -q "format.*json" /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts && grep -q "smsDescription" /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts && grep -q "/sms.do" /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts && ! grep -q "execute(" /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Smsapi.node.ts requestDefaults.qs contains `format: 'json'` (per D-15)
    - Smsapi.node.ts requestDefaults.baseURL is `'https://api.smsapi.pl'`
    - Smsapi.node.ts imports and spreads smsDescription, contactsDescription, groupsDescription, accountDescription
    - Smsapi.node.ts does NOT contain `execute(` (per D-16)
    - Smsapi.node.ts resource selector has options: sms, contact, group, account
    - resources/sms.ts exports `smsDescription` with operations: send, sendGroup, getReport
    - resources/sms.ts send operation routes to POST /sms.do with body params to, message
    - resources/sms.ts send has Additional Fields collection with from, encoding, date, test — routing on each leaf
    - resources/contacts.ts exports `contactsDescription` with operations: list, create, update, delete
    - resources/contacts.ts routes to /contacts endpoints
    - resources/groups.ts routes to GET /contacts/groups
    - resources/account.ts routes to GET /profile
  </acceptance_criteria>
  <done>SMSAPI node with 4 resources (SMS, Contacts, Groups, Account) and 9 operations, all declarative with format=json in requestDefaults</done>
</task>

</tasks>

<verification>
- `grep -r "format.*json" packages/n8n-nodes-smsapi/nodes/Smsapi/Smsapi.node.ts` confirms format=json at node level
- `grep -r "execute(" packages/n8n-nodes-smsapi/nodes/Smsapi/` returns NO matches
- `grep -c "routing" packages/n8n-nodes-smsapi/nodes/Smsapi/resources/sms.ts` returns multiple matches
- `node scripts/verify-packages.js` passes with both packages present
</verification>

<success_criteria>
Complete SMSAPI package: credential with Bearer auth + test, declarative node with 4 resources split into files, 9 operations with proper routing, format=json in requestDefaults, codex, placeholder icon, valid package.json.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-05-SUMMARY.md`
</output>
