---
phase: 01-monorepo-bootstrap-smsapi-ceidg
plan: 10
type: execute
wave: 6
depends_on: [01-08, 01-09]
files_modified: []
autonomous: false
requirements: [INFRA-04]

must_haves:
  truths:
    - "CEIDG node built and linked appears in n8n node picker"
    - "SMSAPI node built and linked appears in n8n node picker"
    - "pnpm link workflow connects packages to n8n custom nodes directory"
    - "CEIDG npm publish --dry-run --provenance exits 0 (guinea pig per D-18)"
    - "Publish workflow YAML is valid and ready for tag-triggered publish"
  artifacts: []
  key_links:
    - from: "packages/n8n-nodes-ceidg/dist/"
      to: "~/.n8n/custom/"
      via: "pnpm link"
      pattern: "n8n-nodes-ceidg"
---

<objective>
Validate the full pipeline: build CEIDG, link to n8n, verify node discovery in n8n picker. Run CEIDG publish dry-run before SMSAPI (per D-18). Verify publish workflow is ready.

Purpose: Per D-18, CEIDG is the publish pipeline guinea pig. This plan validates the end-to-end flow from build to n8n node discovery to publish dry-run. Also validates pnpm link compatibility (D-02).
Output: Confirmed node discovery in n8n; validated publish readiness via dry-run
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link CEIDG and SMSAPI packages to n8n custom nodes</name>
  <files></files>
  <read_first>
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg/package.json
    /Users/mploneczka/projects/n8n/.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-CONTEXT.md
  </read_first>
  <action>
Per D-02, validate pnpm link compatibility with n8n custom nodes.

1. Ensure build is current: `cd /Users/mploneczka/projects/n8n && pnpm run build:all`

2. Create n8n custom nodes directory if needed:
   ```bash
   mkdir -p ~/.n8n/custom
   cd ~/.n8n/custom
   npm init -y 2>/dev/null || true
   ```

3. Link CEIDG package:
   ```bash
   cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg
   pnpm link --global
   cd ~/.n8n/custom
   pnpm link --global n8n-nodes-ceidg
   ```

   If `pnpm link` does not work with n8n (n8n uses npm to resolve custom nodes), try npm link instead:
   ```bash
   cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg
   npm link
   cd ~/.n8n/custom
   npm link n8n-nodes-ceidg
   ```

4. Verify the link exists:
   ```bash
   ls -la ~/.n8n/custom/node_modules/n8n-nodes-ceidg
   ```
   Should be a symlink pointing to the package.

5. Also link SMSAPI:
   ```bash
   cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-smsapi
   npm link
   cd ~/.n8n/custom
   npm link n8n-nodes-smsapi
   ```

6. Document which link method worked (pnpm link vs npm link) for STATE.md update.
  </action>
  <verify>
    <automated>ls -la ~/.n8n/custom/node_modules/n8n-nodes-ceidg 2>/dev/null && echo "LINK EXISTS" || echo "NO LINK"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.n8n/custom/node_modules/n8n-nodes-ceidg` exists (symlink or real)
    - `~/.n8n/custom/node_modules/n8n-nodes-smsapi` exists
    - Both symlinks point to the correct package directories
  </acceptance_criteria>
  <done>Both node packages linked to n8n custom nodes directory</done>
</task>

<task type="auto">
  <name>Task 2: Run CEIDG publish dry-run with provenance (D-18 guinea pig)</name>
  <files></files>
  <read_first>
    /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg/package.json
  </read_first>
  <action>
Per D-18, CEIDG is the publish pipeline guinea pig. Run a publish dry-run to verify the package is correctly structured for npm publication with provenance.

1. Change to CEIDG package directory:
   ```bash
   cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg
   ```

2. Run publish dry-run:
   ```bash
   npm publish --dry-run 2>&1
   ```
   This will NOT actually publish, but will:
   - Validate package.json fields (name, version, description, license, etc.)
   - Show the tarball contents (files that would be published)
   - Verify the `n8n` field references exist in dist/
   - Flag any issues with the package structure

3. Verify the dry-run output:
   - Exit code must be 0
   - Output should list dist/ files including `.node.js` and `.credentials.js`
   - No warnings about missing fields

Note: `--provenance` flag requires OIDC token from GitHub Actions, so it will not work locally. The dry-run without `--provenance` validates package structure. Provenance is verified in CI via `.github/workflows/publish.yml`.

If dry-run fails, fix package.json issues (missing fields, incorrect paths, etc.) before proceeding.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-ceidg && npm publish --dry-run 2>&1 && echo "DRY-RUN PASS" || echo "DRY-RUN FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npm publish --dry-run` exits with code 0 for n8n-nodes-ceidg
    - Output lists dist/ files
    - No errors about missing package.json fields
  </acceptance_criteria>
  <done>CEIDG publish dry-run passes, validating package structure before SMSAPI (per D-18)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify nodes appear in n8n</name>
  <files></files>
  <action>
Human verification checkpoint. Start n8n and verify both CEIDG and SMSAPI nodes appear in the node picker with correct operations and credentials.

Steps for user:
1. Start n8n: `npx n8n start` (or `docker compose up` if using Docker)
2. Open http://localhost:5678
3. Create a new workflow
4. Click "+" to add a node
5. Search for "CEIDG" — should appear with placeholder icon
6. Search for "SMSAPI" — should appear with placeholder icon
7. Click CEIDG — verify 3 operations (Search by NIP, Search by Name, Get)
8. Click SMSAPI — verify 4 resources (SMS, Contact, Group, Account)
9. Check SMSAPI > SMS > Send — verify fields: to, message, Additional Fields
10. Check credentials — both CEIDG API and SMSAPI API credential types should be available
  </action>
  <verify>
    <automated>echo "MANUAL CHECKPOINT — user verifies nodes in n8n UI"</automated>
  </verify>
  <acceptance_criteria>
    - CEIDG node visible in n8n node picker
    - SMSAPI node visible in n8n node picker
    - CEIDG shows 3 operations
    - SMSAPI shows 4 resources with correct operations
    - Both credential types available in n8n credentials UI
  </acceptance_criteria>
  <done>User confirms both nodes appear correctly in n8n with all operations and credentials</done>
</task>

</tasks>

<verification>
- Both nodes visible in n8n node picker
- CEIDG shows 3 operations
- SMSAPI shows 4 resources with 9 operations total
- Credentials types are available
- CEIDG publish dry-run passed before human checkpoint (D-18)
</verification>

<success_criteria>
CEIDG and SMSAPI nodes discovered by n8n, operations match requirements, credentials available. CEIDG publish dry-run validates package structure (D-18). pnpm/npm link compatibility confirmed. Phase 1 pipeline validated end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-bootstrap-smsapi-ceidg/01-10-SUMMARY.md`
</output>
