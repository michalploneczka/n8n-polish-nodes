---
phase: 02-fakturownia
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts
  - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts
  - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts
autonomous: true
requirements:
  - FAKT-05
  - FAKT-06

must_haves:
  truths:
    - "User can list clients with returnAll toggle and pagination"
    - "User can get a client by ID"
    - "User can create a client with name, tax_no, and additional fields"
    - "User can list products with returnAll toggle and pagination"
    - "User can create a product with name, code, price_net, and tax"
  artifacts:
    - path: "packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts"
      provides: "Client field descriptions"
      exports: ["clientOperations", "clientFields"]
    - path: "packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts"
      provides: "Product field descriptions"
      exports: ["productOperations", "productFields"]
  key_links:
    - from: "Fakturownia.node.ts"
      to: "resources/clients.ts"
      via: "import { clientOperations, clientFields }"
      pattern: "clientOperations"
    - from: "Fakturownia.node.ts"
      to: "resources/products.ts"
      via: "import { productOperations, productFields }"
      pattern: "productOperations"
---

<objective>
Add Clients and Products resources to the Fakturownia node with field descriptions and execute() dispatch logic.

Purpose: Completes the Fakturownia node with secondary resources. Clients (List, Get, Create) and Products (List, Create) round out the invoicing workflow.
Output: Full Fakturownia node with all 3 resources (invoice, client, product) operational.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fakturownia/02-RESEARCH.md
@.planning/phases/02-fakturownia/02-02-SUMMARY.md

<interfaces>
From packages/n8n-nodes-fakturownia/nodes/Fakturownia/GenericFunctions.ts:
```typescript
export async function fakturowniaApiRequest(this: IExecuteFunctions, method, endpoint, body?, qs?, options?): Promise<any>;
export async function fakturowniaApiRequestAllItems(this: IExecuteFunctions, method, endpoint, body?, qs?): Promise<IDataObject[]>;
```

From packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/invoices.ts (pattern to follow):
```typescript
export const invoiceOperations: INodeProperties;  // operation selector with displayOptions
export const invoiceFields: INodeProperties[];     // field definitions array
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clients and Products resource descriptions</name>
  <files>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts
  </files>
  <read_first>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/invoices.ts,
    .planning/phases/02-fakturownia/02-RESEARCH.md
  </read_first>
  <action>
    **clients.ts** -- export `clientOperations` and `clientFields`:

    Operations (displayOptions show resource: ['client']):
    - `list` (name: 'List', description: 'Get a list of clients')
    - `get` (name: 'Get', description: 'Get a client by ID')
    - `create` (name: 'Create', description: 'Create a new client')

    Fields:
    - `clientId` (string, required) -- for get operation. displayOptions show operation: ['get']
    - `returnAll` (boolean, default false) -- for list operation
    - `limit` (number, default 50, minValue 1) -- for list when returnAll=false
    - `name` (string, required) -- for create operation. Description: 'Full company or person name'
    - `taxNo` (string, required) -- for create operation. Description: 'Tax identification number (NIP)'
    - `additionalFields` (collection, placeholder 'Add Field') -- for create operation. Contains:
      - `email` (string) -- Client email
      - `phone` (string) -- Phone number
      - `street` (string) -- Street address
      - `post_code` (string) -- Postal code
      - `city` (string) -- City
      - `country` (string) -- Country (ISO code, e.g., PL)
      - `shortcut` (string) -- Short client identifier
      - `note` (string) -- Additional notes

    **products.ts** -- export `productOperations` and `productFields`:

    Operations (displayOptions show resource: ['product']):
    - `list` (name: 'List', description: 'Get a list of products')
    - `create` (name: 'Create', description: 'Create a new product')

    Fields:
    - `returnAll` (boolean, default false) -- for list operation
    - `limit` (number, default 50, minValue 1) -- for list when returnAll=false
    - `productName` (string, required) -- for create operation. property name: 'productName'. Description: 'Product name'
    - `totalPriceGross` (number, required) -- for create operation. Description: 'Total gross price'
    - `tax` (number, required) -- for create operation. Description: 'Tax rate percentage (e.g., 23 for 23%)'
    - `additionalFields` (collection, placeholder 'Add Field') -- for create operation. Contains:
      - `code` (string) -- Product code/SKU
      - `quantity_unit` (string, default 'szt.') -- Unit of measure
      - `description` (string) -- Product description
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n && npx tsc --project packages/n8n-nodes-fakturownia/tsconfig.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts contains "export const clientOperations"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts contains "export const clientFields"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts contains "'list'"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts contains "'create'"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts contains "tax_no"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts contains "export const productOperations"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts contains "export const productFields"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts contains "totalPriceGross"
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Client descriptions define list/get/create operations with name, taxNo, address fields. Product descriptions define list/create operations with productName, totalPriceGross, tax fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire client and product into node -- resource selector + execute() branches</name>
  <files>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts
  </files>
  <read_first>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/products.ts
  </read_first>
  <action>
    Update Fakturownia.node.ts to add client and product support. This involves TWO changes:

    **1. Update resource selector to include client and product options:**
    Find the resource selector in the properties array and add the two new options:
    ```typescript
    {
      displayName: 'Resource',
      name: 'resource',
      type: 'options',
      noDataExpression: true,
      options: [
        { name: 'Client', value: 'client' },
        { name: 'Invoice', value: 'invoice' },
        { name: 'Product', value: 'product' },
      ],
      default: 'invoice',
    }
    ```
    This is the correct time to add these options because their field descriptions now exist (created in Task 1).

    **2. Add imports at top:**
    ```typescript
    import { clientOperations, clientFields } from './resources/clients';
    import { productOperations, productFields } from './resources/products';
    ```

    **3. Add to properties array (after invoiceFields spread):**
    ```typescript
    ...clientOperations ? [clientOperations] : [],
    ...clientFields,
    ...productOperations ? [productOperations] : [],
    ...productFields,
    ```
    (Follow the exact same pattern used for invoiceOperations/invoiceFields)

    **4. Add client dispatch branches inside the for loop, after the invoice block:**

    **client + list:**
    - Call `fakturowniaApiRequestAllItems.call(this, 'GET', '/clients.json')`
    - Push each as `{ json: item }`

    **client + get:**
    - `const clientId = this.getNodeParameter('clientId', i)`
    - Call `fakturowniaApiRequest.call(this, 'GET', \`/clients/${clientId}.json\`)`
    - Push `{ json: response }`

    **client + create:**
    - `const name = this.getNodeParameter('name', i) as string`
    - `const taxNo = this.getNodeParameter('taxNo', i) as string`
    - `const additionalFields = this.getNodeParameter('additionalFields', i, {}) as IDataObject`
    - Build body: `{ client: { name, tax_no: taxNo, ...additionalFields } }`
    - Call `fakturowniaApiRequest.call(this, 'POST', '/clients.json', body)`
    - Push `{ json: response }`

    **5. Add product dispatch branches:**

    **product + list:**
    - Call `fakturowniaApiRequestAllItems.call(this, 'GET', '/products.json')`
    - Push each as `{ json: item }`

    **product + create:**
    - `const productName = this.getNodeParameter('productName', i) as string`
    - `const totalPriceGross = this.getNodeParameter('totalPriceGross', i) as number`
    - `const tax = this.getNodeParameter('tax', i) as number`
    - `const additionalFields = this.getNodeParameter('additionalFields', i, {}) as IDataObject`
    - Build body: `{ product: { name: productName, total_price_gross: totalPriceGross, tax, ...additionalFields } }`
    - Call `fakturowniaApiRequest.call(this, 'POST', '/products.json', body)`
    - Push `{ json: response }`

    The existing "Unsupported resource/operation" throw at the end of the dispatch chain already covers any unrecognized combinations.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n && npx tsc --project packages/n8n-nodes-fakturownia/tsconfig.json --noEmit 2>&1 | head -20 && cd packages/n8n-nodes-fakturownia && npx jest --config jest.config.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "import { clientOperations, clientFields }"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "import { productOperations, productFields }"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "resource === 'client'"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "resource === 'product'"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "/clients.json"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "/products.json"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "tax_no: taxNo"
    - packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts contains "total_price_gross"
    - Resource selector now contains "'client'" and "'product'" values
    - tsc --noEmit exits 0
    - jest exits 0 (Wave 0 tests still pass)
  </acceptance_criteria>
  <done>Fakturownia node handles all 3 resources (invoice: 7 ops, client: 3 ops, product: 2 ops) via execute() dispatch. Resource selector updated to include client and product. Client create sends {client: {name, tax_no, ...}} to /clients.json. Product create sends {product: {name, total_price_gross, tax, ...}} to /products.json.</done>
</task>

</tasks>

<verification>
- `npx tsc --project packages/n8n-nodes-fakturownia/tsconfig.json --noEmit` exits 0
- `cd packages/n8n-nodes-fakturownia && npx jest --config jest.config.js` exits 0
- Fakturownia.node.ts imports and spreads all 3 resource description sets
- Resource selector includes all 3 resources (invoice, client, product)
- execute() has dispatch branches for client (list, get, create) and product (list, create)
</verification>

<success_criteria>
- Client resource has List (paginated), Get, Create operations
- Product resource has List (paginated), Create operations
- Client create sends name + tax_no + additionalFields to /clients.json
- Product create sends productName + totalPriceGross + tax to /products.json
- All list operations use fakturowniaApiRequestAllItems for pagination
- Resource selector correctly lists all 3 resources
- TypeScript compiles without errors
- Wave 0 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-fakturownia/02-03-SUMMARY.md`
</output>
