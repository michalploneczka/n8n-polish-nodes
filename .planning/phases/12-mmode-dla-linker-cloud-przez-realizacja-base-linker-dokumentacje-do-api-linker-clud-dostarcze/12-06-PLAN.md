---
phase: 12-linker-cloud
plan: 06
type: execute
wave: 4
depends_on: [12-05]
files_modified:
  - packages/n8n-nodes-linkercloud/__tests__/LinkerCloud.test.ts
  - packages/n8n-nodes-linkercloud/nodes/LinkerCloud/LinkerCloud.node.json
  - packages/n8n-nodes-linkercloud/icons/linkercloud.svg
  - packages/n8n-nodes-linkercloud/README.md
autonomous: true
requirements: [LC-10]

must_haves:
  truths:
    - "All nock-based tests pass for core operations"
    - "npm run build compiles without errors"
    - "npm run lint passes without errors"
    - "Codex metadata registers node correctly"
    - "README documents all resources and operations"
  artifacts:
    - path: "packages/n8n-nodes-linkercloud/__tests__/LinkerCloud.test.ts"
      provides: "Nock-based HTTP contract tests"
      contains: "nock"
    - path: "packages/n8n-nodes-linkercloud/nodes/LinkerCloud/LinkerCloud.node.json"
      provides: "Codex metadata for n8n node picker"
      contains: "codexVersion"
    - path: "packages/n8n-nodes-linkercloud/README.md"
      provides: "Package documentation"
      contains: "Linker Cloud"
  key_links:
    - from: "__tests__/LinkerCloud.test.ts"
      to: "GenericFunctions.ts"
      via: "nock mocks httpRequest calls"
      pattern: "nock.*linker"
---

<objective>
Finalize the Linker Cloud package with nock-based tests for core operations, codex metadata, SVG icon, README, and full build verification.

Purpose: Tests ensure API contracts are correct. Package metadata enables discovery in n8n node picker.
Output: Complete, publishable n8n community node package.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-mmode-dla-linker-cloud-przez-realizacja-base-linker-dokumentacje-do-api-linker-clud-dostarcze/12-RESEARCH.md
@.planning/phases/12-mmode-dla-linker-cloud-przez-realizacja-base-linker-dokumentacje-do-api-linker-clud-dostarcze/12-01-SUMMARY.md

<interfaces>
<!-- Test pattern from Fakturownia -->
From packages/n8n-nodes-fakturownia/__tests__/: uses createMockExecuteFunctions from @n8n-polish-nodes/test-utils, nock for HTTP mocking, tests verify correct endpoint URLs, methods, query params, and response handling.

<!-- Codex pattern -->
From packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.json:
```json
{
  "node": "n8n-nodes-fakturownia.fakturownia",
  "nodeVersion": "1.0",
  "codexVersion": "1.0",
  "categories": ["Finance & Accounting"],
  "subcategories": { "Finance & Accounting": ["Invoices"] },
  "resources": { "primaryDocumentation": [{ "url": "https://..." }] }
}
```

<!-- Expected API calls to test -->
- GET https://{domain}/public-api/v1/orders?apikey={key}&limit=100&offset=0
- POST https://{domain}/public-api/v1/orders?apikey={key} with OrderType body
- GET https://{domain}/public-api/v1/products?apikey={key}&limit=100&offset=0
- PUT https://{domain}/public-api/v1/products-stocks?apikey={key} with StockUpdateRequest
- POST https://{domain}/public-api/v1/deliveries?apikey={key} with ShipmentType body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nock-based tests for core operations</name>
  <files>packages/n8n-nodes-linkercloud/__tests__/LinkerCloud.test.ts</files>
  <read_first>
    packages/n8n-nodes-fakturownia/__tests__/Fakturownia.test.ts,
    packages/n8n-nodes-linkercloud/nodes/LinkerCloud/LinkerCloud.node.ts,
    packages/n8n-nodes-linkercloud/nodes/LinkerCloud/GenericFunctions.ts,
    shared/test-utils/index.ts
  </read_first>
  <action>
Create test file following the Fakturownia test pattern. Use `createMockExecuteFunctions` from `@n8n-polish-nodes/test-utils` and `nock` for HTTP mocking.

**Test domain:** `test-company.linker.shop`
**Test API key:** `test-api-key-123`

Mock `getCredentials('linkerCloudApi')` to return `{ domain: 'test-company.linker.shop', apiKey: 'test-api-key-123' }`.

**Test cases (minimum 8):**

1. **Order List** -- GET /public-api/v1/orders with apikey param, verifies pagination params (offset=0, limit=100)
2. **Order Get** -- GET /public-api/v1/orders/123 with apikey param
3. **Order Create** -- POST /public-api/v1/orders with body containing required fields (orderDate, items, etc.), verify items have default empty arrays
4. **Order Cancel** -- PUT /public-api/v1/orders/123/cancel with apikey param
5. **Product List** -- GET /public-api/v1/products with apikey param
6. **Product Create** -- POST /public-api/v1/products, verify 9 boolean defaults (false) and 4 array defaults ([])
7. **Stock Update** -- PUT /public-api/v1/products-stocks with { items: [{sku, totalQuantity}] }
8. **Shipment Create** -- POST /public-api/v1/deliveries with ShipmentType body
9. **Error handling** -- Verify NodeApiError is thrown on 500 response with clear message
10. **API key in query params** -- Verify all requests include ?apikey=test-api-key-123

Each test:
- Set up nock interceptor for the exact URL `https://test-company.linker.shop/public-api/v1/...` with query matching `apikey: 'test-api-key-123'`
- Mock `getNodeParameter` for resource, operation, and field values
- Mock `getInputData` to return one item
- Create node instance and call execute
- Assert response matches nock reply
- Call `nock.cleanAll()` in afterEach

Structure tests in describe blocks per resource: 'Order', 'Product', 'Stock', 'Shipment'.

Use `this.helpers.httpRequest` mock pattern from Fakturownia tests (mock the helpers object on the mock execution context).
  </action>
  <verify>
    <automated>cd packages/n8n-nodes-linkercloud && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file imports from @n8n-polish-nodes/test-utils
    - Test file uses nock to mock HTTP requests
    - At least 8 test cases covering Order, Product, Stock, Shipment
    - All tests verify apikey query parameter is present
    - Order Create test verifies items default arrays (serial_numbers, custom_properties, source_data, batch_numbers)
    - Product Create test verifies 9 boolean defaults and 4 array defaults
    - Error handling test verifies NodeApiError message format
    - `npm test` passes with all tests green
  </acceptance_criteria>
  <done>Nock-based tests pass for all core operations, verifying API URL construction, query params, request bodies, and error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Codex, icon, README, and build verification</name>
  <files>
    packages/n8n-nodes-linkercloud/nodes/LinkerCloud/LinkerCloud.node.json,
    packages/n8n-nodes-linkercloud/icons/linkercloud.svg,
    packages/n8n-nodes-linkercloud/README.md
  </files>
  <read_first>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.json,
    packages/n8n-nodes-fakturownia/README.md,
    packages/n8n-nodes-linkercloud/package.json
  </read_first>
  <action>
**LinkerCloud.node.json (codex):**
```json
{
  "node": "n8n-nodes-linkercloud.linkerCloud",
  "nodeVersion": "1.0",
  "codexVersion": "1.0",
  "categories": ["Sales"],
  "subcategories": {
    "Sales": ["Fulfillment"]
  },
  "resources": {
    "primaryDocumentation": [
      { "url": "https://linkercloud.com" }
    ]
  }
}
```

**linkercloud.svg:** Create a proper 60x60 SVG icon. Use a simple design: blue (#2563EB) rounded rectangle background with white "LC" text centered. Format:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" viewBox="0 0 60 60">
  <rect width="60" height="60" rx="8" fill="#2563EB"/>
  <text x="30" y="38" font-family="Arial, sans-serif" font-size="22" font-weight="bold" fill="white" text-anchor="middle">LC</text>
</svg>
```

**README.md:** Follow the SMSAPI/Fakturownia README pattern:

# n8n-nodes-linkercloud

n8n community node for [Linker Cloud](https://linkercloud.com) - Polish WMS/OMS fulfillment platform.

## Operations

| Resource | Operations |
|----------|-----------|
| Order | List, Get, Create, Update, Cancel |
| Product | List, Create, Update |
| Stock | List, Update |
| Shipment | Create, Create by Order Number, Get Label, Get Status, Cancel |
| Inbound Order | List, Get, Create, Update, Confirm |
| Order Return | List, Get, Create, Accept |

## Credentials

1. Get your API key from your Linker Cloud operator or customer service
2. In n8n, create new Linker Cloud API credentials
3. Enter your domain (e.g., `your-company.linker.shop` or `api-demo.linker.shop` for testing)
4. Enter your API key

## Installation

### Community Node (recommended)
1. Go to **Settings > Community Nodes**
2. Select **Install**
3. Enter `n8n-nodes-linkercloud`
4. Agree to risks and install

### Manual
```bash
cd ~/.n8n/custom
npm install n8n-nodes-linkercloud
```

## Notes
- All dates use format `YYYY-MM-DD HH:mm:ss`
- Order items require `serial_numbers`, `custom_properties`, `source_data`, `batch_numbers` arrays (pass empty `[]` if not used)
- Product creation requires 9 boolean flags (default to `false`) and 4 array fields (default to `[]`)
- Stock update is a batch operation accepting multiple SKU+quantity pairs
- Label download returns binary data (PDF or PNG)

## Links
- [Linker Cloud Platform](https://linkercloud.com)
- [n8n Community Nodes](https://docs.n8n.io/integrations/community-nodes/)

## License
MIT

Then run full build and lint:
```bash
cd packages/n8n-nodes-linkercloud && npm run build && npm run lint
```

Fix any lint errors that arise (likely eslint-disable comments needed for manual auth pattern in GenericFunctions.ts).
  </action>
  <verify>
    <automated>cd packages/n8n-nodes-linkercloud && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - LinkerCloud.node.json contains "n8n-nodes-linkercloud.linkerCloud"
    - LinkerCloud.node.json has categories ["Sales"] and subcategories with Fulfillment
    - linkercloud.svg exists and is valid SVG with width="60" height="60"
    - README.md contains operations table with all 6 resources
    - README.md contains credentials setup instructions
    - README.md contains installation instructions (community + manual)
    - `npm run build` completes without errors
    - `npm run lint` completes without errors
    - dist/ directory contains compiled JS files
  </acceptance_criteria>
  <done>Package is complete with codex metadata, icon, documentation, and passes build + lint. Ready for testing and publication.</done>
</task>

</tasks>

<verification>
- `cd packages/n8n-nodes-linkercloud && npm test` -- all tests pass
- `cd packages/n8n-nodes-linkercloud && npm run build` -- compiles cleanly
- `cd packages/n8n-nodes-linkercloud && npm run lint` -- no errors
- `ls packages/n8n-nodes-linkercloud/dist/` -- compiled files exist
</verification>

<success_criteria>
- 8+ nock tests pass covering Order, Product, Stock, Shipment operations
- Build produces dist/ with all compiled files
- Lint passes with zero errors
- Codex registers node in Sales > Fulfillment category
- README documents all 6 resources with operations table
</success_criteria>

<output>
After completion, create `.planning/phases/12-mmode-dla-linker-cloud-przez-realizacja-base-linker-dokumentacje-do-api-linker-clud-dostarcze/12-06-SUMMARY.md`
</output>
