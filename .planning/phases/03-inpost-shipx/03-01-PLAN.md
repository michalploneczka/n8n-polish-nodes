---
phase: 03-inpost-shipx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/n8n-nodes-inpost/package.json
  - packages/n8n-nodes-inpost/tsconfig.json
  - packages/n8n-nodes-inpost/jest.config.js
  - packages/n8n-nodes-inpost/eslint.config.mjs
  - packages/n8n-nodes-inpost/credentials/InpostApi.credentials.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts
autonomous: true
requirements:
  - INPOST-01
  - INPOST-02
  - INPOST-07

must_haves:
  truths:
    - "InPost package scaffolded with build/lint/test scripts that reference correct paths"
    - "Credentials accept apiToken, organizationId, and environment toggle (sandbox/production)"
    - "GenericFunctions provides inpostApiRequest with environment-based URL switching and inpostApiRequestAllItems with page/per_page pagination"
    - "Shipment Create operation sends correct nested body with receiver, parcels, custom_attributes, cod, insurance"
    - "Node is programmatic (execute method) with resource/operation routing"
  artifacts:
    - path: "packages/n8n-nodes-inpost/package.json"
      provides: "Package scaffold with n8n node config"
      contains: "n8n-community-node-package"
    - path: "packages/n8n-nodes-inpost/credentials/InpostApi.credentials.ts"
      provides: "Bearer + orgId + environment credentials"
      exports: ["InpostApi"]
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts"
      provides: "API request helpers"
      exports: ["inpostApiRequest", "inpostApiRequestAllItems"]
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts"
      provides: "Main node with execute()"
      exports: ["Inpost"]
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts"
      provides: "Shipment field definitions"
      exports: ["shipmentOperations", "shipmentFields"]
  key_links:
    - from: "Inpost.node.ts"
      to: "GenericFunctions.ts"
      via: "import inpostApiRequest"
      pattern: "import.*inpostApiRequest.*GenericFunctions"
    - from: "Inpost.node.ts"
      to: "resources/shipments.ts"
      via: "import shipmentOperations, shipmentFields"
      pattern: "import.*shipmentOperations.*resources/shipments"
    - from: "GenericFunctions.ts"
      to: "InpostApi credentials"
      via: "getCredentials('inpostApi')"
      pattern: "getCredentials\\('inpostApi'\\)"
---

<objective>
Scaffold the n8n-nodes-inpost package and implement the core infrastructure: credentials with environment toggle, GenericFunctions with paginated API helpers, and the Shipment Create operation with complex nested body assembly.

Purpose: Establishes the foundation that all subsequent operations build on -- the authenticated API client, URL switching, and the most complex operation (shipment creation with nested receiver/parcels/cod/insurance).
Output: Buildable package with credentials, GenericFunctions, and working Shipment Create route in execute().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-inpost-shipx/03-RESEARCH.md

@packages/n8n-nodes-fakturownia/package.json
@packages/n8n-nodes-fakturownia/tsconfig.json
@packages/n8n-nodes-fakturownia/jest.config.js
@packages/n8n-nodes-fakturownia/eslint.config.mjs
@packages/n8n-nodes-fakturownia/credentials/FakturowniaApi.credentials.ts
@packages/n8n-nodes-fakturownia/nodes/Fakturownia/GenericFunctions.ts
@packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts
@packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/invoices.ts

<interfaces>
<!-- Fakturownia patterns to replicate for InPost -->

From package.json:
- scripts: build = "n8n-node build && node ../../scripts/copy-codex.js"
- n8n.n8nNodesApiVersion: 1, strict: false
- peerDependencies: { "n8n-workflow": "*" }

From tsconfig.json:
- extends: "../../tsconfig.base.json"
- include: ["credentials/**/*", "nodes/**/*", "nodes/**/*.json", "package.json"]

From eslint.config.mjs:
- import { configWithoutCloudSupport } from '@n8n/node-cli/eslint';
- export default [{ ignores: ['__tests__/**'] }, ...configWithoutCloudSupport];

From jest.config.js:
- const base = require('../../jest.config.base.js');
- module.exports = { ...base, rootDir: '.', moduleNameMapper: { '^@n8n-polish-nodes/test-utils$': '<rootDir>/../../shared/test-utils' }, globals: { 'ts-jest': { diagnostics: false } } };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold + credentials + GenericFunctions</name>
  <files>
    packages/n8n-nodes-inpost/package.json,
    packages/n8n-nodes-inpost/tsconfig.json,
    packages/n8n-nodes-inpost/jest.config.js,
    packages/n8n-nodes-inpost/eslint.config.mjs,
    packages/n8n-nodes-inpost/credentials/InpostApi.credentials.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts
  </files>
  <read_first>
    packages/n8n-nodes-fakturownia/package.json,
    packages/n8n-nodes-fakturownia/tsconfig.json,
    packages/n8n-nodes-fakturownia/jest.config.js,
    packages/n8n-nodes-fakturownia/eslint.config.mjs,
    packages/n8n-nodes-fakturownia/credentials/FakturowniaApi.credentials.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/GenericFunctions.ts
  </read_first>
  <action>
    Create the n8n-nodes-inpost package following the exact Fakturownia pattern:

    **package.json** -- copy Fakturownia's structure, change:
    - name: "n8n-nodes-inpost"
    - description: "n8n community node for InPost ShipX - Polish parcel delivery and Paczkomaty"
    - keywords: ["n8n-community-node-package", "n8n", "inpost", "shipx", "paczkomaty", "parcel", "shipping", "poland", "polish"]
    - n8n.credentials: ["dist/credentials/InpostApi.credentials.js"]
    - n8n.nodes: ["dist/nodes/Inpost/Inpost.node.js"]
    - repository.directory: "packages/n8n-nodes-inpost"
    - Same scripts, devDependencies, peerDependencies as Fakturownia

    **tsconfig.json** -- identical to Fakturownia's (extends ../../tsconfig.base.json, outDir ./dist/)

    **jest.config.js** -- identical to Fakturownia's (base config, rootDir '.', moduleNameMapper for test-utils, ts-jest diagnostics:false)

    **eslint.config.mjs** -- identical to Fakturownia's (configWithoutCloudSupport, ignores __tests__)

    **InpostApi.credentials.ts** -- implements ICredentialType with:
    - name = 'inpostApi'
    - displayName = 'InPost ShipX API'
    - icon = 'file:../icons/inpost.svg' as const
    - documentationUrl = 'https://dokumentacja-inpost.atlassian.net/wiki/spaces/PL'
    - Properties:
      1. apiToken (string, password:true, required) -- "Bearer token from InPost Manager Panel"
      2. organizationId (string, required) -- "Organization ID from InPost Manager Panel (numeric)"
      3. environment (options, required, default 'sandbox') -- options: [{ name: 'Sandbox', value: 'sandbox' }, { name: 'Production', value: 'production' }]
    - test: ICredentialTestRequest with expression-based URL:
      url: '={{$credentials.environment === "sandbox" ? "https://sandbox-api-shipx-pl.easypack24.net" : "https://api-shipx-pl.easypack24.net"}}/v1/organizations'
      headers: { Authorization: '={{`Bearer ${$credentials.apiToken}`}}' }
      method: 'GET'

    **GenericFunctions.ts** -- two exported functions:
    1. `inpostApiRequest(method, endpoint, body={}, qs={}, options={})`:
       - Gets credentials via `this.getCredentials('inpostApi')`
       - Determines baseUrl: sandbox='https://sandbox-api-shipx-pl.easypack24.net', production='https://api-shipx-pl.easypack24.net'
       - Builds IHttpRequestOptions: method, url=baseUrl+endpoint, headers={Authorization: `Bearer ${apiToken}`}, qs, json:true, ...options
       - Adds body for POST/PUT/DELETE methods
       - eslint-disable for no-http-request-with-manual-auth (justified: environment-based dynamic URL)
       - Catches errors, wraps in NodeApiError with message "InPost API error: ..."
       - Returns IDataObject | IDataObject[] | Buffer | undefined

    2. `inpostApiRequestAllItems(method, endpoint, body={}, qs={})`:
       - Gets returnAll and limit from node parameters (index 0)
       - Uses per_page=25, page starts at 1
       - Loops: calls inpostApiRequest, extracts `items` from response envelope
       - Break conditions: !returnAll && allItems.length >= limit (slice to limit), or page >= total_pages, or items.length < perPage
       - Returns IDataObject[]

    Import types: IDataObject, IExecuteFunctions, IHttpRequestMethods, IHttpRequestOptions, JsonObject from 'n8n-workflow'. Import NodeApiError from 'n8n-workflow'.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-inpost && cat package.json | grep '"n8n-community-node-package"' && cat credentials/InpostApi.credentials.ts | grep "name = 'inpostApi'" && cat nodes/Inpost/GenericFunctions.ts | grep "inpostApiRequestAllItems"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "n8n-community-node-package" in keywords
    - package.json contains "n8n-nodes-inpost" as name
    - package.json n8n.credentials contains "dist/credentials/InpostApi.credentials.js"
    - InpostApi.credentials.ts exports class InpostApi with name='inpostApi'
    - InpostApi.credentials.ts contains environment options field with 'sandbox' and 'production' values
    - InpostApi.credentials.ts contains organizationId property
    - InpostApi.credentials.ts contains test: ICredentialTestRequest with expression URL
    - GenericFunctions.ts exports inpostApiRequest and inpostApiRequestAllItems
    - GenericFunctions.ts contains 'sandbox-api-shipx-pl.easypack24.net' and 'api-shipx-pl.easypack24.net'
    - GenericFunctions.ts contains eslint-disable comment for no-http-request-with-manual-auth
    - GenericFunctions.ts contains NodeApiError wrapping
    - GenericFunctions.ts pagination extracts response.items (not treats response as array)
  </acceptance_criteria>
  <done>Package scaffold complete with credentials (3 fields + env toggle + test request), GenericFunctions (API request + paginated helper), and all config files matching Fakturownia pattern</done>
</task>

<task type="auto">
  <name>Task 2: Inpost.node.ts + shipments resource with Create operation</name>
  <files>
    packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts
  </files>
  <read_first>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/invoices.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts,
    packages/n8n-nodes-inpost/credentials/InpostApi.credentials.ts
  </read_first>
  <action>
    Create the main node class and the shipments resource file with Create operation:

    **resources/shipments.ts** -- exports shipmentOperations (INodeProperties[]) and shipmentFields (INodeProperties[]):

    Operations array (type:'options', displayName:'Operation', name:'operation', displayOptions show resource=['shipment']):
    - { name: 'Create', value: 'create', action: 'Create a shipment' }
    - { name: 'Get', value: 'get', action: 'Get a shipment' }
    - { name: 'Get Many', value: 'getAll', action: 'Get many shipments' }
    - { name: 'Cancel', value: 'cancel', action: 'Cancel a shipment' }
    - { name: 'Get Label', value: 'getLabel', action: 'Get shipment label' }

    Fields array for Create operation (all with displayOptions show resource=['shipment'], operation=['create']):

    1. service (options, required, default 'inpost_locker_standard'):
       - InPost Locker Standard (inpost_locker_standard)
       - InPost Locker Express (inpost_locker_express)
       - InPost Courier Standard (inpost_courier_standard)
       - InPost Courier Express (inpost_courier_express)
       - InPost Courier Palette (inpost_courier_palette)

    2. targetPoint (string, required, placeholder 'KRA010', description 'Paczkomat code'):
       - displayOptions: show service=['inpost_locker_standard', 'inpost_locker_express']

    3. receiver (fixedCollection, multipleValues:false, required):
       - Single group 'receiverDetails' with fields:
         - name (string, default '', description 'Full name')
         - email (string, default '', description 'Email address')
         - phone (string, default '', required, description 'Phone number (9 digits)')
         - first_name (string, default '', description 'First name')
         - last_name (string, default '', description 'Last name')
         - company_name (string, default '', description 'Company name')

    4. receiverAddress (fixedCollection, multipleValues:false):
       - displayOptions: show service=['inpost_courier_standard', 'inpost_courier_express', 'inpost_courier_palette']
       - Single group 'addressDetails' with fields:
         - street (string, required)
         - building_number (string, required)
         - city (string, required)
         - post_code (string, required, placeholder '00-000')
         - country_code (string, default 'PL')

    5. parcels (fixedCollection, multipleValues:true, required):
       - Group 'parcelValues' with fields:
         - template (options, default 'small') with: small, medium, large, custom
           - displayOptions: show for locker services only
         - length (number, default 80, description 'Length in mm'):
           - displayOptions: show for courier services OR when template='custom'
         - width (number, default 360, description 'Width in mm')
         - height (number, default 640, description 'Height in mm')
         - weight (number, default 5, description 'Weight in kg')
         - isNonStandard (boolean, default false)

    6. additionalFields (collection, default {}):
       - codAmount (number, description 'Cash on delivery amount in PLN')
       - insuranceAmount (number, description 'Insurance amount in PLN')
       - reference (string, description 'Custom reference (e.g., order number)')
       - sendingMethod (options: dispatch_order, parcel_locker, default 'dispatch_order')
       - senderDetails (fixedCollection, multipleValues:false) with: name, email, phone, company_name

    Also add field stubs for Get, GetAll, Cancel, GetLabel operations (just the ID/pagination fields -- actual execute logic in Plan 02):
    - shipmentId (string, required) for get, cancel, getLabel operations
    - returnAll (boolean) and limit (number, default 50) for getAll
    - labelFormat (options: normal=A4, A6) for getLabel, default 'normal'

    **Inpost.node.ts** -- implements INodeType with execute() method:

    Description:
    - displayName: 'InPost ShipX'
    - name: 'inpost'
    - icon: 'file:../../icons/inpost.svg'
    - group: ['output']
    - version: 1
    - subtitle: '={{$parameter["operation"] + ": " + $parameter["resource"]}}'
    - description: 'Manage shipments, track packages, and find Paczkomaty via InPost ShipX API'
    - defaults: { name: 'InPost ShipX' }
    - usableAsTool: true
    - inputs: [NodeConnectionTypes.Main], outputs: [NodeConnectionTypes.Main]
    - credentials: [{ name: 'inpostApi', required: true }]
    - properties: [resourceSelector, ...shipmentOperations, ...shipmentFields]
      (resource selector has options: { name: 'Shipment', value: 'shipment' } -- points and tracking added in Plan 02)

    Import and expose GenericFunctions as static class properties to avoid unused-import tsc error:
    ```
    static inpostApiRequest = inpostApiRequest;
    static inpostApiRequestAllItems = inpostApiRequestAllItems;
    ```

    execute() method:
    - Get items via this.getInputData()
    - Loop over items with continueOnFail() support (try/catch per item)
    - Get resource and operation from parameters
    - For resource='shipment', operation='create':
      1. Get credentials for organizationId: `const credentials = await this.getCredentials('inpostApi'); const orgId = credentials.organizationId as string;`
      2. Get service from parameter
      3. Build receiver object from fixedCollection: `const receiverData = this.getNodeParameter('receiver', i) as IDataObject; const receiver = (receiverData.receiverDetails || {}) as IDataObject;`
      4. For courier services, merge receiverAddress: `const addressData = this.getNodeParameter('receiverAddress', i, {}) as IDataObject; if (addressData.addressDetails) { receiver.address = addressData.addressDetails; }`
      5. Build parcels array from fixedCollection: map parcelValues to { dimensions: {length, width, height, unit:'mm'}, weight: {amount, unit:'kg'}, is_non_standard }. If template is set and not 'custom', use { template } instead of dimensions.
      6. Build body: { service, receiver, parcels }
      7. If locker service: `body.custom_attributes = { target_point: targetPoint, sending_method: sendingMethod }`
      8. If additionalFields.codAmount: `body.cod = { amount: codAmount, currency: 'PLN' }`
      9. If additionalFields.insuranceAmount: `body.insurance = { amount: insuranceAmount, currency: 'PLN' }`
      10. If additionalFields.reference: `body.reference = reference`
      11. If additionalFields.senderDetails: `body.sender = senderDetails.senderValues`
      12. POST to `/v1/organizations/${orgId}/shipments`
      13. Push response as json to returnData
    - For other operations: add TODO comments (implemented in Plan 02)
    - In catch: if continueOnFail, push error item; else throw
    - Return [returnData]
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-inpost && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Inpost.node.ts exports class Inpost implementing INodeType
    - Inpost.node.ts contains `execute()` method (programmatic style per INPOST-07)
    - Inpost.node.ts contains displayName 'InPost ShipX'
    - Inpost.node.ts contains credentials name 'inpostApi'
    - Inpost.node.ts contains static inpostApiRequest and static inpostApiRequestAllItems properties
    - Inpost.node.ts imports shipmentOperations and shipmentFields from resources/shipments
    - resources/shipments.ts exports shipmentOperations and shipmentFields
    - resources/shipments.ts contains 5 operations: create, get, getAll, cancel, getLabel
    - resources/shipments.ts contains fixedCollection for receiver with receiverDetails group
    - resources/shipments.ts contains fixedCollection for parcels with parcelValues group
    - resources/shipments.ts contains service options with 5 InPost service types
    - resources/shipments.ts contains targetPoint field with displayOptions for locker services
    - resources/shipments.ts contains receiverAddress fixedCollection with displayOptions for courier services
    - resources/shipments.ts contains additionalFields with codAmount, insuranceAmount, reference
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>Node class with execute() routes shipment create through GenericFunctions with correct nested body assembly for receiver, parcels, target_point, cod, insurance; TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `cd packages/n8n-nodes-inpost && npx tsc --noEmit` compiles without errors
- package.json has correct n8n paths and keywords
- Credentials have 3 fields (apiToken, organizationId, environment) plus test request
- GenericFunctions has both inpostApiRequest and inpostApiRequestAllItems
- Node has execute() with shipment create body assembly
</verification>

<success_criteria>
- Package scaffold matches Fakturownia pattern exactly (config files, scripts, dependencies)
- Credentials support sandbox/production toggle with expression-based test URL
- GenericFunctions provides authenticated API client with pagination
- Shipment Create assembles complex nested body (receiver, parcels, custom_attributes, cod, insurance)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-inpost-shipx/03-01-SUMMARY.md`
</output>
