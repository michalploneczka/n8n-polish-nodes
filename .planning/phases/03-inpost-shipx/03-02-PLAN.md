---
phase: 03-inpost-shipx
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/resources/points.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/resources/tracking.ts
autonomous: true
requirements:
  - INPOST-03
  - INPOST-04
  - INPOST-05
  - INPOST-06
  - INPOST-08

must_haves:
  truths:
    - "User can get a shipment by ID and list shipments with pagination (returnAll toggle)"
    - "User can cancel a shipment by ID"
    - "User can download a shipping label as binary PDF in A4 or A6 format"
    - "User can list and get Paczkomaty (parcel locker points)"
    - "User can track a shipment by tracking number"
  artifacts:
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/resources/points.ts"
      provides: "Points resource field definitions"
      exports: ["pointOperations", "pointFields"]
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/resources/tracking.ts"
      provides: "Tracking resource field definitions"
      exports: ["trackingOperations", "trackingFields"]
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts"
      provides: "Complete execute() with all operations"
      contains: "prepareBinaryData"
  key_links:
    - from: "Inpost.node.ts"
      to: "resources/points.ts"
      via: "import pointOperations, pointFields"
      pattern: "import.*pointOperations.*resources/points"
    - from: "Inpost.node.ts"
      to: "resources/tracking.ts"
      via: "import trackingOperations, trackingFields"
      pattern: "import.*trackingOperations.*resources/tracking"
    - from: "Inpost.node.ts (getLabel)"
      to: "this.helpers.prepareBinaryData"
      via: "binary PDF download"
      pattern: "prepareBinaryData"
---

<objective>
Implement all remaining InPost operations: Shipments Get/List/Cancel/GetLabel, Points List/Get, and Tracking Get. Wires all resources into the main node's execute() method.

Purpose: Completes the full InPost API surface -- after this plan, every documented operation works.
Output: Fully operational node with all 8 operations across 3 resources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inpost-shipx/03-RESEARCH.md
@.planning/phases/03-inpost-shipx/03-01-SUMMARY.md

@packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts
@packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts
@packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts
@packages/n8n-nodes-inpost/credentials/InpostApi.credentials.ts

<interfaces>
<!-- From Plan 01 outputs that this plan consumes -->

From GenericFunctions.ts:
```typescript
export async function inpostApiRequest(
  this: IExecuteFunctions,
  method: IHttpRequestMethods,
  endpoint: string,
  body: IDataObject = {},
  qs: IDataObject = {},
  options: IDataObject = {},
): Promise<IDataObject | IDataObject[] | Buffer | undefined>;

export async function inpostApiRequestAllItems(
  this: IExecuteFunctions,
  method: IHttpRequestMethods,
  endpoint: string,
  body: IDataObject = {},
  qs: IDataObject = {},
): Promise<IDataObject[]>;
```

From Fakturownia binary download pattern (reference):
```typescript
// PDF download with encoding:arraybuffer and json:false
const response = await fakturowniaApiRequest.call(this, 'GET', endpoint, {}, {}, {
  encoding: 'arraybuffer',
  json: false,
});
const binaryData = await this.helpers.prepareBinaryData(
  Buffer.from(response as unknown as ArrayBuffer),
  filename,
  'application/pdf',
);
returnData.push({ json: { id }, binary: { data: binaryData } });
```

API Endpoints (from RESEARCH.md):
- GET /v1/shipments/{id} -- get shipment
- GET /v1/organizations/{org_id}/shipments -- list shipments (paginated)
- DELETE /v1/organizations/{org_id}/shipments/{id} -- cancel
- GET /v1/organizations/{org_id}/shipments/{id}/label?format=pdf&type=normal -- label
- GET /v1/points -- list points (paginated)
- GET /v1/points/{name} -- get point
- GET /v1/tracking/{tracking_number} -- tracking
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Points and Tracking resource files</name>
  <files>
    packages/n8n-nodes-inpost/nodes/Inpost/resources/points.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/tracking.ts
  </files>
  <read_first>
    packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/resources/clients.ts
  </read_first>
  <action>
    Create two new resource definition files following the shipments.ts pattern:

    **resources/points.ts** -- exports pointOperations (INodeProperties[]) and pointFields (INodeProperties[]):

    Operations (displayOptions show resource=['point']):
    - { name: 'Get', value: 'get', action: 'Get a parcel locker point' }
    - { name: 'Get Many', value: 'getAll', action: 'Get many parcel locker points' }

    Fields for Get (displayOptions show resource=['point'], operation=['get']):
    - pointName (string, required, placeholder 'KRA010', description 'Point name/code (e.g., KRA010)')

    Fields for GetAll (displayOptions show resource=['point'], operation=['getAll']):
    - returnAll (boolean, default false)
    - limit (number, default 50, typeOptions minValue:1, displayOptions show returnAll=[false])
    - filters (collection, default {}):
      - name (string, description 'Filter by point name (partial match)')
      - city (string, description 'Filter by city name')
      - type (options: parcel_locker/parcel_locker_superpop/pop, description 'Filter by point type')
      - functions (options: parcel_collect/parcel_send, description 'Filter by function')

    **resources/tracking.ts** -- exports trackingOperations (INodeProperties[]) and trackingFields (INodeProperties[]):

    Operations (displayOptions show resource=['tracking']):
    - { name: 'Get', value: 'get', action: 'Get tracking status' }

    Fields for Get (displayOptions show resource=['tracking'], operation=['get']):
    - trackingNumber (string, required, description 'Shipment tracking number')

    Both files: import type { INodeProperties } from 'n8n-workflow';
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-inpost && cat nodes/Inpost/resources/points.ts | grep "export const pointOperations" && cat nodes/Inpost/resources/tracking.ts | grep "export const trackingOperations"</automated>
  </verify>
  <acceptance_criteria>
    - points.ts exports pointOperations and pointFields
    - points.ts contains get and getAll operations
    - points.ts contains pointName field for get operation
    - points.ts contains returnAll, limit, and filters (name, city, type, functions) for getAll
    - tracking.ts exports trackingOperations and trackingFields
    - tracking.ts contains get operation with trackingNumber field
  </acceptance_criteria>
  <done>Points resource (2 operations with filters) and Tracking resource (1 operation) field definitions complete</done>
</task>

<task type="auto">
  <name>Task 2: Wire all operations into execute() method</name>
  <files>
    packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts
  </files>
  <read_first>
    packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/points.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/tracking.ts,
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.ts
  </read_first>
  <action>
    Update Inpost.node.ts to:

    1. **Add imports** for points and tracking resources:
       ```
       import { pointOperations, pointFields } from './resources/points';
       import { trackingOperations, trackingFields } from './resources/tracking';
       ```

    2. **Update resource selector** to include all 3 resources:
       - { name: 'Shipment', value: 'shipment' }
       - { name: 'Point', value: 'point', description: 'Paczkomaty parcel locker points' }
       - { name: 'Tracking', value: 'tracking' }

    3. **Update properties array** to spread all resource definitions:
       ```
       properties: [
         resourceSelector,
         ...shipmentOperations, ...shipmentFields,
         ...pointOperations, ...pointFields,
         ...trackingOperations, ...trackingFields,
       ]
       ```

    4. **Implement all remaining operations in execute()**:

       For resource='shipment':
       - operation='get':
         Get shipmentId from parameter. GET `/v1/shipments/${shipmentId}`. Push response as json.

       - operation='getAll':
         Get orgId from credentials. Call inpostApiRequestAllItems with GET `/v1/organizations/${orgId}/shipments`. Push each item as json.

       - operation='cancel':
         Get orgId from credentials, shipmentId from parameter. DELETE `/v1/organizations/${orgId}/shipments/${shipmentId}`. Push { success: true, shipmentId } as json.

       - operation='getLabel':
         Get orgId from credentials, shipmentId from parameter, labelFormat from parameter (default 'normal').
         Call inpostApiRequest with GET `/v1/organizations/${orgId}/shipments/${shipmentId}/label`, qs: { format: 'pdf', type: labelFormat }, options: { encoding: 'arraybuffer', json: false }.
         Convert response to binary: `const binaryData = await this.helpers.prepareBinaryData(Buffer.from(response as unknown as ArrayBuffer), 'label-' + shipmentId + '.pdf', 'application/pdf');`
         Push: `{ json: { shipmentId }, binary: { data: binaryData } }`

       For resource='point':
       - operation='get':
         Get pointName from parameter. GET `/v1/points/${pointName}`. Push response as json.

       - operation='getAll':
         Build qs from filters (name, city, type, functions -- only add if non-empty).
         Call inpostApiRequestAllItems with GET `/v1/points`. Push each item as json.

       For resource='tracking':
       - operation='get':
         Get trackingNumber from parameter. GET `/v1/tracking/${trackingNumber}`. Push response as json.

    5. **Ensure continueOnFail()** wraps every operation in the per-item try/catch. On error with continueOnFail: push `{ json: { error: (error as Error).message } }`.

    After updating the node, verify shipments.ts has all needed field stubs (shipmentId, returnAll, limit, labelFormat) from Plan 01. If any are missing, add them.
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-inpost && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Inpost.node.ts imports pointOperations, pointFields, trackingOperations, trackingFields
    - Inpost.node.ts resource selector has 3 options: shipment, point, tracking
    - Inpost.node.ts execute() handles shipment.get (GET /v1/shipments/{id})
    - Inpost.node.ts execute() handles shipment.getAll (GET /v1/organizations/{orgId}/shipments with pagination)
    - Inpost.node.ts execute() handles shipment.cancel (DELETE /v1/organizations/{orgId}/shipments/{id})
    - Inpost.node.ts execute() handles shipment.getLabel with prepareBinaryData (encoding:arraybuffer, json:false)
    - Inpost.node.ts execute() handles point.get (GET /v1/points/{name})
    - Inpost.node.ts execute() handles point.getAll (GET /v1/points with pagination and filters)
    - Inpost.node.ts execute() handles tracking.get (GET /v1/tracking/{trackingNumber})
    - Inpost.node.ts contains continueOnFail() pattern in catch block
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>All 8 operations across 3 resources (Shipment, Point, Tracking) wired into execute() with correct endpoint paths, pagination, binary label download, and continueOnFail error handling; TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `cd packages/n8n-nodes-inpost && npx tsc --noEmit` compiles without errors
- All 3 resources (shipment, point, tracking) present in node description
- All 8 operations implemented in execute() method
- Binary label download uses prepareBinaryData with arraybuffer encoding
- Points getAll supports filters (name, city, type, functions)
</verification>

<success_criteria>
- All InPost API operations from RESEARCH.md implemented
- Shipment Get/List/Cancel use correct org-scoped endpoints
- Label download produces binary PDF output matching Fakturownia pattern
- Points and Tracking resources complete
- continueOnFail support throughout execute()
</success_criteria>

<output>
After completion, create `.planning/phases/03-inpost-shipx/03-02-SUMMARY.md`
</output>
