---
phase: 03-inpost-shipx
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/n8n-nodes-inpost/__tests__/Inpost.node.test.ts
  - packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.json
  - packages/n8n-nodes-inpost/icons/inpost.svg
  - packages/n8n-nodes-inpost/README.md
autonomous: true
requirements:
  - INPOST-09

must_haves:
  truths:
    - "All operations have nock-based tests (happy path) that pass"
    - "Error handling test verifies NodeApiError wrapping"
    - "Codex file has correct node reference and categories"
    - "SVG icon exists at correct path"
    - "README documents all operations, credentials setup, and rate limits"
    - "npm run build, npm run lint, and npm run test all pass"
  artifacts:
    - path: "packages/n8n-nodes-inpost/__tests__/Inpost.node.test.ts"
      provides: "Test suite for all operations"
      min_lines: 100
    - path: "packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.json"
      provides: "Codex metadata"
      contains: "n8n-nodes-inpost.inpost"
    - path: "packages/n8n-nodes-inpost/icons/inpost.svg"
      provides: "Node icon"
    - path: "packages/n8n-nodes-inpost/README.md"
      provides: "Package documentation"
      contains: "InPost ShipX"
  key_links:
    - from: "__tests__/Inpost.node.test.ts"
      to: "nodes/Inpost/Inpost.node.ts"
      via: "import and instantiate Inpost class"
      pattern: "import.*Inpost.*Inpost\\.node"
    - from: "Inpost.node.json"
      to: "Inpost.node.ts"
      via: "node reference in codex"
      pattern: "n8n-nodes-inpost\\.inpost"
---

<objective>
Complete the InPost package with comprehensive nock-based tests for all operations, codex metadata, SVG icon, README documentation, and full build/lint/test verification.

Purpose: Makes the package publish-ready with tests proving all operations work, proper metadata for n8n node picker, and documentation for users.
Output: Fully tested, documented, build-clean package ready for npm publish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inpost-shipx/03-RESEARCH.md
@.planning/phases/03-inpost-shipx/03-01-SUMMARY.md
@.planning/phases/03-inpost-shipx/03-02-SUMMARY.md

@packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts
@packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts
@packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts
@packages/n8n-nodes-inpost/nodes/Inpost/resources/points.ts
@packages/n8n-nodes-inpost/nodes/Inpost/resources/tracking.ts
@packages/n8n-nodes-inpost/credentials/InpostApi.credentials.ts
@packages/n8n-nodes-inpost/package.json

<interfaces>
<!-- From Fakturownia test pattern -->

From packages/n8n-nodes-fakturownia/__tests__/Fakturownia.node.test.ts (pattern to follow):
- Uses createMockExecuteFunctions from @n8n-polish-nodes/test-utils
- Uses nock to intercept HTTP requests to API base URL
- Tests: description structure (has properties, has credentials), each operation happy path, error handling
- Mock credentials via mockReturnValue on getCredentials
- Mock node parameters via mockReturnValue on getNodeParameter

Test structure:
```typescript
import nock from 'nock';
import { createMockExecuteFunctions } from '@n8n-polish-nodes/test-utils';
import { Inpost } from '../nodes/Inpost/Inpost.node';

const BASE_URL = 'https://sandbox-api-shipx-pl.easypack24.net';
const MOCK_CREDENTIALS = { apiToken: 'test-token', organizationId: '12345', environment: 'sandbox' };
```

Codex pattern (from Fakturownia):
```json
{
  "node": "n8n-nodes-inpost.inpost",
  "nodeVersion": "1.0",
  "codexVersion": "1.0",
  "categories": ["Miscellaneous"],
  "subcategories": { "Miscellaneous": ["Other"] },
  "resources": { "primaryDocumentation": [{ "url": "..." }] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nock-based tests for all operations</name>
  <files>
    packages/n8n-nodes-inpost/__tests__/Inpost.node.test.ts
  </files>
  <read_first>
    packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/GenericFunctions.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/shipments.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/points.ts,
    packages/n8n-nodes-inpost/nodes/Inpost/resources/tracking.ts,
    packages/n8n-nodes-fakturownia/__tests__/Fakturownia.node.test.ts
  </read_first>
  <action>
    Create comprehensive test file following the Fakturownia test pattern exactly.

    Setup:
    - import nock from 'nock'
    - import { createMockExecuteFunctions } from '@n8n-polish-nodes/test-utils'
    - import { Inpost } from '../nodes/Inpost/Inpost.node'
    - const BASE_URL = 'https://sandbox-api-shipx-pl.easypack24.net'
    - const MOCK_CREDENTIALS = { apiToken: 'test-token', organizationId: '12345', environment: 'sandbox' }
    - afterEach(() => nock.cleanAll())

    Test groups:

    **1. describe('Node Description')**
    - 'should have correct properties' -- check node.description has displayName, properties, credentials
    - 'should have all resources' -- check resource options include shipment, point, tracking
    - 'should use execute method' -- verify typeof node.execute === 'function'

    **2. describe('Shipment Operations')**
    - 'should create a shipment' -- mock POST /v1/organizations/12345/shipments, verify request body has service, receiver, parcels. Return mock shipment with id.
      Mock params: resource='shipment', operation='create', service='inpost_locker_standard', targetPoint='KRA010', receiver fixedCollection data, parcels fixedCollection data, additionalFields={}
    - 'should get a shipment' -- mock GET /v1/shipments/123. Mock params: shipmentId='123'
    - 'should list shipments' -- mock GET /v1/organizations/12345/shipments?page=1&per_page=25 returning { items: [{id:1},{id:2}], total_pages: 1, count: 2 }. Mock params: returnAll=false, limit=50
    - 'should cancel a shipment' -- mock DELETE /v1/organizations/12345/shipments/123. Mock params: shipmentId='123'
    - 'should get label as binary PDF' -- mock GET /v1/organizations/12345/shipments/123/label with query format=pdf&type=normal, return Buffer.from('fake-pdf'). Verify prepareBinaryData called. Mock params: shipmentId='123', labelFormat='normal'

    **3. describe('Point Operations')**
    - 'should get a point' -- mock GET /v1/points/KRA010. Return mock point data {name:'KRA010', type:'parcel_locker'}. Mock params: pointName='KRA010'
    - 'should list points' -- mock GET /v1/points?page=1&per_page=25 returning { items: [{name:'KRA010'}], total_pages: 1 }. Mock params: returnAll=false, limit=50, filters={}

    **4. describe('Tracking')**
    - 'should get tracking status' -- mock GET /v1/tracking/TRACK123. Return mock tracking data. Mock params: trackingNumber='TRACK123'

    **5. describe('Error Handling')**
    - 'should throw NodeApiError on API error' -- mock GET /v1/shipments/999 returning 404 with {error:'not_found',message:'Shipment not found'}. Verify execute throws with message containing 'InPost API error'
    - 'should handle continueOnFail' -- mock returning error, set continueOnFail to return true. Verify error item in output instead of thrown error.

    For label test: mock this.helpers.prepareBinaryData to return a mock binary object. Verify it receives a Buffer, filename 'label-123.pdf', and mimetype 'application/pdf'.

    Use the exact mock pattern from Fakturownia tests:
    - `mockFunctions.getCredentials.mockResolvedValue(MOCK_CREDENTIALS)`
    - `mockFunctions.getNodeParameter.mockImplementation((name: string) => { ... })` with a switch/map for parameter values
    - `mockFunctions.getInputData.mockReturnValue([{ json: {} }])`
    - `mockFunctions.continueOnFail.mockReturnValue(false)` (or true for continueOnFail test)
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-inpost && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Test file imports createMockExecuteFunctions from @n8n-polish-nodes/test-utils
    - Test file imports Inpost from ../nodes/Inpost/Inpost.node
    - At least 10 test cases covering all operations (create, get, getAll, cancel, getLabel, point.get, point.getAll, tracking.get, error, continueOnFail)
    - All nock interceptors use BASE_URL 'https://sandbox-api-shipx-pl.easypack24.net'
    - Label test verifies prepareBinaryData is called with Buffer, filename containing 'label-', and 'application/pdf'
    - Error test verifies NodeApiError wrapping
    - All tests pass (npm test exits 0)
  </acceptance_criteria>
  <done>All 10+ test cases pass, covering every operation happy path plus error handling and continueOnFail</done>
</task>

<task type="auto">
  <name>Task 2: Codex, SVG icon, README, and build/lint verification</name>
  <files>
    packages/n8n-nodes-inpost/nodes/Inpost/Inpost.node.json,
    packages/n8n-nodes-inpost/icons/inpost.svg,
    packages/n8n-nodes-inpost/README.md
  </files>
  <read_first>
    packages/n8n-nodes-fakturownia/nodes/Fakturownia/Fakturownia.node.json,
    packages/n8n-nodes-fakturownia/icons/fakturownia.svg,
    packages/n8n-nodes-fakturownia/README.md,
    packages/n8n-nodes-inpost/package.json
  </read_first>
  <action>
    Create three files and verify the full build pipeline:

    **Inpost.node.json** (codex file):
    ```json
    {
      "node": "n8n-nodes-inpost.inpost",
      "nodeVersion": "1.0",
      "codexVersion": "1.0",
      "categories": ["Miscellaneous"],
      "subcategories": {
        "Miscellaneous": ["Other"]
      },
      "resources": {
        "primaryDocumentation": [
          {
            "url": "https://dokumentacja-inpost.atlassian.net/wiki/spaces/PL"
          }
        ]
      }
    }
    ```

    **icons/inpost.svg** -- Create a simple 60x60 SVG with the InPost brand colors (yellow #FFCC00 background, dark text). Use a clean geometric representation:
    - 60x60 viewBox
    - Yellow (#FFCC00) rounded rectangle background
    - Dark (#1D1D1B) text "IP" centered (InPost abbreviation)
    - Keep it simple and recognizable

    **README.md** -- Follow Fakturownia README structure:
    - Title: # n8n-nodes-inpost
    - Badge: npm version badge
    - Description: n8n community node for InPost ShipX - Polish parcel delivery and Paczkomaty
    - Section: ## Operations
      Table with Resource | Operation | Description columns:
      | Shipment | Create | Create a new shipment (locker or courier) |
      | Shipment | Get | Get shipment details by ID |
      | Shipment | Get Many | List shipments with pagination |
      | Shipment | Cancel | Cancel a shipment |
      | Shipment | Get Label | Download shipping label as PDF (A4/A6) |
      | Point | Get | Get parcel locker point details |
      | Point | Get Many | List and filter Paczkomaty points |
      | Tracking | Get | Track shipment by tracking number |
    - Section: ## Credentials
      - API Token: Bearer token from InPost Manager Panel
      - Organization ID: numeric org ID from InPost Manager Panel
      - Environment: Sandbox or Production toggle
      - Note: Sandbox uses sandbox-api-shipx-pl.easypack24.net, Production uses api-shipx-pl.easypack24.net
    - Section: ## Compatibility
      - Tested with n8n 1.x
      - Requires n8n-workflow peer dependency
    - Section: ## Rate Limits (per INPOST-08)
      - InPost ShipX API: 100 requests per minute
      - This node does not implement automatic retry. If you hit rate limits, add a Wait node or reduce batch sizes.
    - Section: ## Resources
      - InPost ShipX API Docs: https://dokumentacja-inpost.atlassian.net/wiki/spaces/PL
      - n8n Community Nodes: link
    - Section: ## Example Workflow
      - JSON code block with a minimal workflow that creates a shipment
    - License: MIT

    **After creating files, run full verification:**
    1. `cd packages/n8n-nodes-inpost && npm run build` -- must succeed
    2. `cd packages/n8n-nodes-inpost && npm run lint` -- must pass (fix any errors)
    3. `cd packages/n8n-nodes-inpost && npm test` -- must pass
    4. Verify dist/ contains compiled files
    5. If lint fails, fix the issues (common: resource files need git force-add due to .gitignore, eslint-disable comments may be needed)
  </action>
  <verify>
    <automated>cd /Users/mploneczka/projects/n8n/packages/n8n-nodes-inpost && npm run build && npm run lint && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Inpost.node.json contains "n8n-nodes-inpost.inpost" as node reference
    - Inpost.node.json contains categories ["Miscellaneous"]
    - icons/inpost.svg exists and contains viewBox="0 0 60 60"
    - icons/inpost.svg contains #FFCC00 (InPost yellow)
    - README.md contains "# n8n-nodes-inpost"
    - README.md contains operations table with all 8 operations
    - README.md contains "Rate Limits" section mentioning 100 requests per minute
    - README.md contains credentials section with apiToken, organizationId, environment
    - npm run build exits 0
    - npm run lint exits 0
    - npm run test exits 0
    - dist/ directory contains compiled .js files
  </acceptance_criteria>
  <done>Package is publish-ready: build/lint/test all pass, codex metadata present, icon visible, README documents all operations and rate limits</done>
</task>

</tasks>

<verification>
- `cd packages/n8n-nodes-inpost && npm run build && npm run lint && npm test` all pass
- Codex file references correct node name
- README covers all 8 operations and rate limit warning
- Icon exists at icons/inpost.svg
</verification>

<success_criteria>
- All tests pass covering every operation
- Build produces clean dist/ output
- Lint passes with zero errors
- Documentation complete with operations table, credentials guide, rate limit warning
- Package ready for npm publish via GitHub Actions
</success_criteria>

<output>
After completion, create `.planning/phases/03-inpost-shipx/03-03-SUMMARY.md`
</output>
