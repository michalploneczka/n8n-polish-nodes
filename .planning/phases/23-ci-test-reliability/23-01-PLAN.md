---
phase: 23-ci-test-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - shared/test-utils/package.json
  - pnpm-lock.yaml
  - __tests__/integration/integration.test.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "test:structural step runs in CI pipeline after build:all and test:all"
    - "nock is explicitly declared as devDependency in shared/test-utils/package.json"
    - "INT-03 workflow import tests fail loudly when auth is not available instead of silently passing"
    - "activateWorkflow in E2E helpers uses POST-first with PATCH fallback (already implemented)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with structural test step"
      contains: "pnpm run test:structural"
    - path: "shared/test-utils/package.json"
      provides: "Explicit nock dependency declaration"
      contains: "nock"
    - path: "__tests__/integration/integration.test.ts"
      provides: "Fail-fast auth assertion for INT-03"
      contains: "throw new Error"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pnpm run test:structural"
      via: "CI step after test:all"
      pattern: "run: pnpm run test:structural"
    - from: "__tests__/integration/integration.test.ts"
      to: "apiKey"
      via: "beforeAll assertion"
      pattern: "throw new Error.*Failed to obtain"
---

<objective>
Close four CI/test reliability gaps identified in v1.0 milestone audit: wire structural tests into CI, declare missing nock dependency, fix silent 401 skip in integration tests, and confirm activateWorkflow fallback (already implemented).

Purpose: Ensure CI catches structural regressions, dependencies are explicit (not relying on hoisting), and integration tests fail visibly when auth setup breaks.
Output: Updated ci.yml, test-utils/package.json, integration.test.ts, and verification that activateWorkflow is correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ci-test-reliability/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire test:structural into CI and declare nock dependency</name>
  <files>.github/workflows/ci.yml, shared/test-utils/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .github/workflows/ci.yml
    - shared/test-utils/package.json
    - package.json (root, to confirm nock version ^14.0.0)
  </read_first>
  <action>
1. In `.github/workflows/ci.yml`, in the `ci` job, add a new step AFTER the `pnpm run test:all` line (line 27) and BEFORE the `node scripts/verify-packages.js` line (line 28):

```yaml
      - run: pnpm run test:structural
```

The final step order in the `ci` job should be:
- pnpm install --frozen-lockfile
- pnpm run lint:all
- pnpm run build:all
- pnpm run test:all
- pnpm run test:structural   <-- NEW
- node scripts/verify-packages.js

2. In `shared/test-utils/package.json`, add `nock` to devDependencies with the same version as root package.json (`^14.0.0`). The resulting file:

```json
{
  "name": "@n8n-polish-nodes/test-utils",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "devDependencies": {
    "n8n-workflow": "*",
    "nock": "^14.0.0"
  }
}
```

3. Run `pnpm install` to update `pnpm-lock.yaml` with the new dependency resolution. Verify it completes without errors.
  </action>
  <verify>
    <automated>grep -q "test:structural" .github/workflows/ci.yml && node -e "const p=require('./shared/test-utils/package.json'); if(!p.devDependencies.nock) process.exit(1)" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains the line `- run: pnpm run test:structural` between `test:all` and `verify-packages.js`
    - shared/test-utils/package.json contains `"nock": "^14.0.0"` in devDependencies
    - pnpm-lock.yaml is updated (pnpm install completes without error)
    - `pnpm run test:structural` still passes locally
  </acceptance_criteria>
  <done>CI pipeline runs 499 structural tests on every push/PR; nock is explicitly declared in test-utils package.json</done>
</task>

<task type="auto">
  <name>Task 2: Fix INT-03 silent 401 skip and verify activateWorkflow</name>
  <files>__tests__/integration/integration.test.ts</files>
  <read_first>
    - __tests__/integration/integration.test.ts
    - __tests__/e2e/helpers.ts (to confirm activateWorkflow POST+PATCH fallback is already implemented)
  </read_first>
  <action>
1. In `__tests__/integration/integration.test.ts`, at the END of the `beforeAll` block (after both the owner setup attempt and the login fallback attempt, before the closing `}, 90000);`), add a fail-fast assertion:

```typescript
    // Fail fast if auth not obtained -- INT-03 tests must not silently skip
    if (!apiKey) {
      throw new Error(
        'Failed to obtain n8n API key -- owner setup and login both failed. ' +
        'INT-03 workflow import tests require authentication in the Docker test environment.',
      );
    }
```

2. In the `describe('Smoke Workflow Import (INT-03)')` block, remove the 401 silent skip block (lines 148-154). The code to REMOVE is:

```typescript
        if (response.status === 401) {
          // Auth required but not available — skip gracefully
          console.warn(
            `Skipping ${fixtureFile}: API requires authentication (401)`,
          );
          return;
        }
```

After removal, the test body should go directly from the fetch call to `expect(response.status).toBe(200)`. Since beforeAll now guarantees apiKey is defined, the X-N8N-API-KEY header will always be present.

3. Verify activateWorkflow in `__tests__/e2e/helpers.ts` -- this is already correctly implemented with POST /activate first and PATCH fallback on 404/405 (lines 120-160). No code change needed. This task confirms the audit finding is resolved.
  </action>
  <verify>
    <automated>grep -q "Failed to obtain n8n API key" __tests__/integration/integration.test.ts && ! grep -q "Skipping.*API requires authentication" __tests__/integration/integration.test.ts && grep -q "activateEndpoint.*post.*patch" __tests__/e2e/helpers.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/integration/integration.test.ts beforeAll contains `throw new Error('Failed to obtain n8n API key`
    - __tests__/integration/integration.test.ts does NOT contain `Skipping ${fixtureFile}: API requires authentication (401)` or similar 401 skip pattern
    - __tests__/integration/integration.test.ts still contains the `if (apiKey)` header setting (apiKey is now guaranteed by beforeAll)
    - __tests__/e2e/helpers.ts contains `activateEndpoint` with 'post' | 'patch' type and POST-first logic (already present, no change needed)
    - `pnpm run test:all` passes (no regressions in unit tests)
  </acceptance_criteria>
  <done>INT-03 tests fail loudly when auth is unavailable instead of silently passing; activateWorkflow POST+PATCH fallback confirmed already implemented</done>
</task>

</tasks>

<verification>
1. `grep "test:structural" .github/workflows/ci.yml` -- shows the new CI step
2. `node -e "console.log(require('./shared/test-utils/package.json').devDependencies)"` -- shows nock in deps
3. `grep "Failed to obtain" __tests__/integration/integration.test.ts` -- shows fail-fast assertion
4. `grep -c "Skipping.*authentication" __tests__/integration/integration.test.ts` -- returns 0 (removed)
5. `pnpm run test:structural` -- passes (0.8s, 499 tests)
6. `pnpm run test:all` -- passes (no regressions)
</verification>

<success_criteria>
- test:structural is wired into CI pipeline after build:all and test:all
- nock ^14.0.0 is declared in shared/test-utils/package.json devDependencies
- INT-03 tests throw Error in beforeAll if apiKey not obtained (no silent skip)
- activateWorkflow POST+PATCH fallback confirmed correct in E2E helpers
- All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/23-ci-test-reliability/23-01-SUMMARY.md`
</output>
